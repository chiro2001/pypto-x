From 79a7daa1052775651b22e797c346651c70b60070 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Tue, 8 Sep 2026 12:02:20 +0800
Subject: [PATCH 033/111] test(pypto-x): isolate AVX2 objdump instruction
 checks

---
 python/tests/ut/pypto_x/test_cpu_avx2.py | 19 ++++++++++++-------
 1 file changed, 12 insertions(+), 7 deletions(-)

diff --git a/python/tests/ut/pypto_x/test_cpu_avx2.py b/python/tests/ut/pypto_x/test_cpu_avx2.py
index 961db9503..8017f14f7 100644
--- a/python/tests/ut/pypto_x/test_cpu_avx2.py
+++ b/python/tests/ut/pypto_x/test_cpu_avx2.py
@@ -960,8 +960,13 @@ def test_native_variant_marker_binds_metadata_to_binary(tmp_path):
 
 
 def test_objdump_has_ymm_and_no_zmm(tmp_path):
-    _, _, fma_artifact = _compile(_add_program(shape=(19,)), _target("avx2", "fma"), tmp_path / "fma")
-    _, _, no_fma_artifact = _compile(_add_program(shape=(19,)), _target("avx2"), tmp_path / "no-fma")
+    artifact_root = tmp_path / "integration-w3-avx512-final"
+    _, _, fma_artifact = _compile(
+        _add_program(shape=(19,)), _target("avx2", "fma"), artifact_root / "fma"
+    )
+    _, _, no_fma_artifact = _compile(
+        _add_program(shape=(19,)), _target("avx2"), artifact_root / "no-fma"
+    )
     objdump = shutil.which("objdump")
     if objdump is None:
         pytest.skip("GNU objdump is unavailable")
@@ -969,14 +974,15 @@ def test_objdump_has_ymm_and_no_zmm(tmp_path):
         result = subprocess.run(
             [objdump, "-d", path], stdout=subprocess.PIPE, universal_newlines=True, check=True
         )
-        text = result.stdout.lower()
+        instruction_lines = []
         first_bytes = []
-        for line in text.splitlines():
-            match = re.match(r"^\s*[0-9a-f]+:\s+((?:[0-9a-f]{2}\s+)+)", line)
+        for line in result.stdout.lower().splitlines():
+            match = re.match(r"^\s*[0-9a-f]+:\s+((?:[0-9a-f]{2}\s+)+)(.*)$", line)
             if match:
                 first_bytes.append(int(match.group(1).split()[0], 16))
+                instruction_lines.append(match.group(2))
         assert first_bytes
-        return text, first_bytes
+        return "\n".join(instruction_lines), first_bytes
 
     fma_text, fma_first_bytes = disassemble(fma_artifact.path)
     no_fma_text, no_fma_first_bytes = disassemble(no_fma_artifact.path)
@@ -986,7 +992,6 @@ def test_objdump_has_ymm_and_no_zmm(tmp_path):
         assert "ymm" in text
         assert "zmm" not in text
         assert "evex" not in text
-        assert "avx512" not in text
     assert "vfmadd" in fma_text
     assert "vfmadd" not in no_fma_text
 
