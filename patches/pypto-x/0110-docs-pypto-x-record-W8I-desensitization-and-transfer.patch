From 8c8fa5fd411341ac81ab2af79506c82dab84f8a5 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Thu, 10 Sep 2026 23:13:14 +0800
Subject: [PATCH 110/257] docs(pypto-x): record W8I desensitization and
 transfer integrity

---
 tools/a2_vllm_ascend_profiling/assemble_validation.py |  3 +++
 tools/a2_vllm_ascend_profiling/brief.zh-CN.md         | 13 ++++++++++---
 2 files changed, 13 insertions(+), 3 deletions(-)

diff --git a/tools/a2_vllm_ascend_profiling/assemble_validation.py b/tools/a2_vllm_ascend_profiling/assemble_validation.py
index ea3d8e338..3f03ad3e1 100644
--- a/tools/a2_vllm_ascend_profiling/assemble_validation.py
+++ b/tools/a2_vllm_ascend_profiling/assemble_validation.py
@@ -250,6 +250,9 @@ def main(argv=None):
         ),
         "a2_910b.sh --copy 的 scp 在本环境不可用（旧协议/端口问题），改用 ssh -J ... 'cat > / <' 完成双向传输，md5 校验一致；",
         "extract_csv 对 kernel_details/operator_details 做了 top-5000 行截断（按 duration 降序），完整大表留在 A2 trace 内。",
+        "脱敏：a2 日志中 vLLM 自报的容器 IP（hccl distributed_init_method）已替换为 <redacted-ip>；"
+        "原始传输 tar.gz 解包后删除，md5 见 raw/transfer_integrity.json；127.0.0.1 为本地回环测试端口。",
+        "A2 容器时钟比本机真实 UTC 快约 8 小时（date -u 按 CST 输出）；本文件 generated_at 用本机 UTC。",
     ]
     if inventory.get("errors"):
         result["status"] = "PARTIAL"
diff --git a/tools/a2_vllm_ascend_profiling/brief.zh-CN.md b/tools/a2_vllm_ascend_profiling/brief.zh-CN.md
index f2a06b4e3..620ae6d0f 100644
--- a/tools/a2_vllm_ascend_profiling/brief.zh-CN.md
+++ b/tools/a2_vllm_ascend_profiling/brief.zh-CN.md
@@ -129,11 +129,18 @@ sanity              PASS：8 token / 6.64 s（首跑含编译）；
 分支 work/qwen35-a2-vllm-ascend-profiling-baseline（base 0e5a5189142d，未 push、未合并）：
   4f764edac  feat(pypto-x): add version-adaptive A2 vllm-ascend profiling harness (W8I)
   49c116095  chore(pypto-x): export A2 spawn/HF-offline env in W8I orchestrator
-  （+2 个运行时修正：CANN set_env 在 set -u 下、extract_csv 多 run 前缀）
+  9691c4ff1  fix(pypto-x): tolerate CANN set_env under set -u in W8I orchestrator
+  cf10d773a  fix(pypto-x): unique CSV output prefixes per profiling run in W8I extractor
+  f4876f993  feat(pypto-x): add evidence sha256 manifest tool for W8I
   ccec01fa9  fix(pypto-x): skip non-chrome JSON in W8I CSV extractor
+  fb273fbf6  docs(pypto-x): finalize W8I profiling brief, references addendum and validation assembler
   （最终 HEAD 以 git log 为准；本文件定稿时的 HEAD 见 git rev-parse HEAD）
 ```
 
 工作树内：`tools/a2_vllm_ascend_profiling/`（harness + 文档 + 自检 + 装配脚本）。
-证据内：`validation.json`、`brief.zh-CN.md`、`docs-references.md`、`raw/a2_final/`（含 bundle 与原始 JSON）、
-`csv/a2_final/`、`logs/a2_final/`、`evidence_manifest.json`。
+证据内：`validation.json`、`brief.zh-CN.md`、`docs-references.md`、`raw/a2_final/`（原始 JSON +
+`transfer_integrity.json`）、`csv/a2_final/`、`logs/a2_final/`、`evidence_manifest.json`。
+
+**脱敏执行记录**：a2 日志中 vLLM 自报的容器 IP（`179.44.54.246`，hccl init 地址）
+已全部替换为 `<redacted-ip>`；原始传输 tar.gz 在解包后删除，md5 记录在
+`raw/transfer_integrity.json`（08b: 223bd28a…，phase2: 84c62a60…）。
