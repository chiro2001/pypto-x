From 1c28bcc047f5e93d0c97db724fc8beaea05099c0 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Tue, 8 Sep 2026 15:34:13 +0800
Subject: [PATCH 042/250] fix(pypto-x): align GPU option and payload validation

---
 python/pypto/abi/gpu_tokens.py             |  5 ++++-
 python/pypto/backends/gpu/runtime.py       | 17 ++++++++++-------
 python/pypto/compiler/targets/gpu.py       |  2 ++
 python/tests/ut/pypto_x/test_gpu_common.py | 12 +++++++++++-
 4 files changed, 27 insertions(+), 9 deletions(-)

diff --git a/python/pypto/abi/gpu_tokens.py b/python/pypto/abi/gpu_tokens.py
index 4a1d59bef..9f3381328 100644
--- a/python/pypto/abi/gpu_tokens.py
+++ b/python/pypto/abi/gpu_tokens.py
@@ -27,6 +27,9 @@ _VENDOR_TOKEN_RE = re.compile(
     r"(?<![A-Za-z0-9])(?:" + "|".join(re.escape(term) for term in VENDOR_TERMS) + r")(?![A-Za-z0-9])",
     re.IGNORECASE,
 )
+_VENDOR_CAMEL_RE = re.compile(
+    r"(?<![A-Za-z0-9])(?:(?i:" + "|".join(re.escape(term) for term in VENDOR_TERMS) + r"))(?=[A-Z])"
+)
 
 
 def find_vendor_term(value: Any, context: str = "value") -> Optional[str]:
@@ -38,7 +41,7 @@ def find_vendor_term(value: Any, context: str = "value") -> Optional[str]:
     """
 
     if isinstance(value, str):
-        if _VENDOR_TOKEN_RE.search(value):
+        if _VENDOR_TOKEN_RE.search(value) or _VENDOR_CAMEL_RE.search(value):
             return context
         return None
     if isinstance(value, Mapping):
diff --git a/python/pypto/backends/gpu/runtime.py b/python/pypto/backends/gpu/runtime.py
index 01eaf3514..d32547d37 100644
--- a/python/pypto/backends/gpu/runtime.py
+++ b/python/pypto/backends/gpu/runtime.py
@@ -344,13 +344,16 @@ def _decode_artifact(artifact: Artifact) -> LoadedGpuArtifact:
     _artifact_mapping(decoded.get("gpu_program"), "gpu_program")
     for field in ("program_digest", "plan_digest"):
         _artifact_string(decoded.get(field), field)
-    canonical_payload = json.dumps(
-        decoded,
-        ensure_ascii=False,
-        allow_nan=False,
-        sort_keys=True,
-        separators=(",", ":"),
-    ).encode("utf-8")
+    try:
+        canonical_payload = json.dumps(
+            decoded,
+            ensure_ascii=False,
+            allow_nan=False,
+            sort_keys=True,
+            separators=(",", ":"),
+        ).encode("utf-8")
+    except (TypeError, ValueError, UnicodeEncodeError) as exc:
+        raise GpuArtifactError("GPU artifact payload cannot be canonicalized") from exc
     if canonical_payload != payload:
         raise GpuArtifactError("GPU artifact payload is not canonical JSON")
     if decoded.get("artifact_payload_version") != 1:
diff --git a/python/pypto/compiler/targets/gpu.py b/python/pypto/compiler/targets/gpu.py
index ceb47d731..55579e429 100644
--- a/python/pypto/compiler/targets/gpu.py
+++ b/python/pypto/compiler/targets/gpu.py
@@ -61,6 +61,8 @@ class GpuCommonCompiler:
         if options is not None and not isinstance(options, CompileOptions):
             raise TypeError("options must be CompileOptions")
         if options is not None:
+            _reject_vendor_terms(options.artifact_format, "compile_options.artifact_format")
+            _reject_vendor_terms(options.abi_version, "compile_options.abi_version")
             _reject_vendor_terms(options.metadata, "compile_options.metadata")
             _reject_vendor_terms(options.extra, "compile_options.extra")
         target_ir.validate()
diff --git a/python/tests/ut/pypto_x/test_gpu_common.py b/python/tests/ut/pypto_x/test_gpu_common.py
index e84416bb3..d8d7f6a18 100644
--- a/python/tests/ut/pypto_x/test_gpu_common.py
+++ b/python/tests/ut/pypto_x/test_gpu_common.py
@@ -753,6 +753,8 @@ def test_compiler_options_metadata_reserved_and_load_roundtrip():
     assert GpuCommonSimulator().load(option_artifact).target_ir.to_dict() == target_ir.to_dict()
     with pytest.raises(ValueError, match="reserved"):
         compiler.compile(target_ir, CompileOptions(metadata={"compile_options": "user"}))
+    with pytest.raises(ValueError, match="vendor"):
+        compiler.compile(target_ir, CompileOptions(artifact_format="cuda"))
 
 
 @pytest.mark.parametrize(
@@ -782,6 +784,14 @@ def test_malformed_compile_options_fields_are_gpu_artifact_errors():
         GpuCommonSimulator().load(tampered)
 
 
+def test_nan_payload_canonicalization_is_a_gpu_artifact_error():
+    _, _, artifact = _compile(_add_program((3,)))
+    target = dict(json.loads(artifact.payload.decode("utf-8"))["target"])
+    target["attributes"] = {"nan": float("nan")}
+    with pytest.raises(GpuArtifactError, match="canonical"):
+        GpuCommonSimulator().load(_rebuild_artifact(artifact, envelope={"target": target}))
+
+
 def test_artifact_missing_envelope_and_known_metadata_tamper_are_rejected():
     _, _, artifact = _compile(_add_program((3,)))
     decoded = json.loads(artifact.payload.decode("utf-8"))
@@ -827,7 +837,7 @@ def test_core_metadata_tokens_are_boundary_checked_not_substrings():
         metadata={"relationship": "ownership"},
     )
     assert lower_gpu_common(relationship_program, TARGET).plans
-    for value in ("cuda_stream", "hip-target"):
+    for value in ("cuda_stream", "hip-target", "cudaStream", "hipStream"):
         bad_program = _program(
             "bad_vendor",
             program.functions[0].parameters,
