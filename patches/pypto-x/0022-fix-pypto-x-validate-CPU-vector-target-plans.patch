From 4fc388cf5fa53e971ec92373fdbcb7f970d4b35b Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Mon, 7 Sep 2026 03:15:52 +0800
Subject: [PATCH 022/134] fix(pypto-x): validate CPU vector target plans

---
 python/pypto/backends/cpu/vector/runtime.py       |  41 ++++++++++++---
 python/pypto/compiler/targets/cpu_vector.py       |   1 +
 python/pypto/lowering/cpu/vector/plan.py          | 117 +++++++++++++++++++++++++++++++++++++++---
 python/tests/ut/pypto_x/test_cpu_vector_common.py |  84 ++++++++++++++++++++++++++++--
 4 files changed, 223 insertions(+), 20 deletions(-)

diff --git a/python/pypto/backends/cpu/vector/runtime.py b/python/pypto/backends/cpu/vector/runtime.py
index 8fae68c1e..cc13eb8b3 100644
--- a/python/pypto/backends/cpu/vector/runtime.py
+++ b/python/pypto/backends/cpu/vector/runtime.py
@@ -33,7 +33,7 @@ from pypto.lowering.cpu.vector import (
     lower_cpu_vector,
 )
 from pypto.target import TargetSpec
-from pypto.target._utils import mapping_copy, sha256_text
+from pypto.target._utils import sha256_text
 
 from ..tensor import FlatBuffer, HostTensor, _cast_value
 
@@ -155,7 +155,12 @@ class CpuVectorRuntime:
                 raise CpuVectorExecutionError("unsupported parameter kind {!r}".format(parameter.type.kind))
 
         for index, operation in enumerate(function.body.operations):
-            _execute_operation(operation, loaded.target_ir.plans[index], environment, "function {} operation {}".format(function.name, index))
+            _execute_operation(
+                operation,
+                loaded.target_ir.plans[index],
+                environment,
+                "function {} operation {}".format(function.name, index),
+            )
 
         results: List[Any] = []
         output_index = 0
@@ -334,7 +339,9 @@ def _read_tensor(descriptor: TensorDesc, value_type: CoreType, context: str) ->
     expected_count = element_count(expected_shape)
     if len(values) != expected_count:
         raise CpuVectorExecutionError(
-            "{} storage has {} values but shape {} requires {}".format(context, len(values), expected_shape, expected_count)
+            "{} storage has {} values but shape {} requires {}".format(
+                context, len(values), expected_shape, expected_count
+            )
         )
     try:
         return [_cast_value(value, expected_dtype) for value in values]
@@ -496,7 +503,14 @@ def _vector_map(plan: Any, values: Sequence[Any], operation: Callable[[Any], Any
     return result
 
 
-def _execute_binary(name: str, left: _RuntimeValue, right: _RuntimeValue, output_type: CoreType, plan: Any, context: str) -> Any:
+def _execute_binary(
+    name: str,
+    left: _RuntimeValue,
+    right: _RuntimeValue,
+    output_type: CoreType,
+    plan: Any,
+    context: str,
+) -> Any:
     left_values, left_scalar = _value_sequence(left.data, left.type, context + ".left")
     right_values, right_scalar = _value_sequence(right.data, right.type, context + ".right")
     output_is_scalar = _is_scalar(output_type)
@@ -552,9 +566,16 @@ def _execute_reduction(name: str, value: _RuntimeValue, output_type: CoreType, p
             for input_index, input_value in enumerate(input_values):
                 input_coords = _unravel(input_index, input_shape)
                 if reduction.keepdims:
-                    projected = tuple(0 if axis in reduction.axes else input_coords[axis] for axis in range(len(input_shape)))
+                    projected = tuple(
+                        0 if axis in reduction.axes else input_coords[axis]
+                        for axis in range(len(input_shape))
+                    )
                 else:
-                    projected = tuple(input_coords[axis] for axis in range(len(input_shape)) if axis not in reduction.axes)
+                    projected = tuple(
+                        input_coords[axis]
+                        for axis in range(len(input_shape))
+                        if axis not in reduction.axes
+                    )
                 if projected == output_coords:
                     candidates.append(input_value)
             if len(candidates) != reduction.reduction_size:
@@ -583,7 +604,13 @@ def _execute_reduction(name: str, value: _RuntimeValue, output_type: CoreType, p
     return result
 
 
-def _execute_matmul(left: _RuntimeValue, right: _RuntimeValue, output_type: CoreType, plan: Any, context: str) -> List[Any]:
+def _execute_matmul(
+    left: _RuntimeValue,
+    right: _RuntimeValue,
+    output_type: CoreType,
+    plan: Any,
+    context: str,
+) -> List[Any]:
     matmul = plan.matmul
     left_values, left_scalar = _value_sequence(left.data, left.type, context + ".left")
     right_values, right_scalar = _value_sequence(right.data, right.type, context + ".right")
diff --git a/python/pypto/compiler/targets/cpu_vector.py b/python/pypto/compiler/targets/cpu_vector.py
index a60409184..02ce8d84f 100644
--- a/python/pypto/compiler/targets/cpu_vector.py
+++ b/python/pypto/compiler/targets/cpu_vector.py
@@ -42,6 +42,7 @@ class CpuVectorCompiler:
             raise TypeError("CPU vector compile() requires CpuVectorProgram from lower()")
         if options is not None and not isinstance(options, CompileOptions):
             raise TypeError("options must be CompileOptions")
+        target_ir.validate()
 
         program_json = target_ir.program.canonical_json()
         program_payload = json.loads(program_json)
diff --git a/python/pypto/lowering/cpu/vector/plan.py b/python/pypto/lowering/cpu/vector/plan.py
index dd3cd4688..63f2ce511 100644
--- a/python/pypto/lowering/cpu/vector/plan.py
+++ b/python/pypto/lowering/cpu/vector/plan.py
@@ -13,6 +13,7 @@ from __future__ import annotations
 
 from dataclasses import dataclass
 from enum import Enum
+import re
 from typing import Any, Mapping, Optional, Sequence, Tuple
 
 from pypto.core_ir import CoreProgram, CoreType, Operation
@@ -35,6 +36,7 @@ VECTOR_PLAN_VERSION = 1
 VECTOR_LOWERING_VERSION = 1
 CPU_VECTOR_BACKEND = "cpu.vector.common"
 DEFAULT_LANE_COUNT = 4
+_SSA_NAME_RE = re.compile(r"^(?:%[A-Za-z0-9_.$-]+|[A-Za-z_][A-Za-z0-9_.$-]*)$")
 
 
 class CpuVectorError(ValueError):
@@ -71,6 +73,25 @@ def _non_negative_int(value: Any, field: str) -> int:
     return value
 
 
+def _validate_ssa_name(value: Any, field: str) -> str:
+    if not isinstance(value, str) or not _SSA_NAME_RE.fullmatch(value):
+        raise CpuVectorValidationError("{} must be a stable SSA name".format(field))
+    return value
+
+
+def _validate_shape(shape: Sequence[Any], field: str) -> Tuple[int, ...]:
+    try:
+        normalized = tuple(shape)
+    except TypeError as exc:
+        raise CpuVectorValidationError("{} must be a sequence".format(field)) from exc
+    for index, dimension in enumerate(normalized):
+        if isinstance(dimension, bool) or not isinstance(dimension, int) or dimension < 0:
+            raise CpuVectorValidationError(
+                "{}[{}] must be a non-negative integer".format(field, index)
+            )
+    return normalized
+
+
 def _product(values: Sequence[int]) -> int:
     result = 1
     for value in values:
@@ -262,6 +283,8 @@ class TailPlan:
     elements: int
 
     def __post_init__(self) -> None:
+        if not isinstance(self.policy, TailPolicy):
+            raise CpuVectorValidationError("tail.policy must be a TailPolicy")
         _non_negative_int(self.start, "tail.start")
         if isinstance(self.elements, bool) or not isinstance(self.elements, int) or self.elements < 0:
             raise CpuVectorValidationError("tail.elements must be a non-negative integer")
@@ -555,7 +578,11 @@ def _check_layout(
         if not _is_tensor(ref.type):
             continue
         if ref.type.kind == "view":
-            raise CpuVectorUnsupportedError("{} value {!r} is a view; non-contiguous views are unsupported".format(context, ref.name))
+            raise CpuVectorUnsupportedError(
+                "{} value {!r} is a view; non-contiguous views are unsupported".format(
+                    context, ref.name
+                )
+            )
         raw = operation_layout
         if isinstance(layouts, Mapping) and ref.name in layouts:
             raw = layouts[ref.name]
@@ -572,7 +599,11 @@ def _check_layout(
         if axis is not None:
             normalized_axis = axis + len(shape) if axis < 0 else axis
             if normalized_axis != expected_axis:
-                raise CpuVectorUnsupportedError("{} value {!r} contiguous axis is not the innermost axis".format(context, ref.name))
+                raise CpuVectorUnsupportedError(
+                    "{} value {!r} contiguous axis is not the innermost axis".format(
+                        context, ref.name
+                    )
+                )
             axis = normalized_axis
         else:
             axis = expected_axis
@@ -660,24 +691,39 @@ class VectorPlan:
             raise CpuVectorValidationError("unknown vector plan kind {!r}".format(plan_kind))
         if isinstance(operation_index, bool) or not isinstance(operation_index, int) or operation_index < 0:
             raise CpuVectorValidationError("operation_index must be a non-negative integer")
-        if not isinstance(output_name, str) or not output_name:
-            raise CpuVectorValidationError("output_name must be non-empty")
+        if isinstance(input_names, (str, bytes)):
+            raise CpuVectorValidationError("plan.input_names must be a sequence of SSA names")
+        normalized_inputs = tuple(
+            _validate_ssa_name(value, "plan.input_names[{}]".format(index))
+            for index, value in enumerate(input_names)
+        )
+        normalized_output = _validate_ssa_name(output_name, "plan.output_name")
         normalized_dtype = canonical_dtype(dtype)
-        normalized_shape = tuple(shape)
+        normalized_shape = _validate_shape(shape, "plan.shape")
         count = _non_negative_int(element_count, "plan.element_count")
         lanes = _positive_int(lane_count, "plan.lane_count")
         if count != _product(normalized_shape):
             raise CpuVectorValidationError("plan.element_count does not match shape")
+        if not isinstance(layout, str) or not layout.strip():
+            raise CpuVectorValidationError("plan.layout must be a non-empty string")
+        if contiguous_axis is not None:
+            _non_negative_int(contiguous_axis, "plan.contiguous_axis")
+            if not normalized_shape or contiguous_axis >= len(normalized_shape):
+                raise CpuVectorValidationError("plan.contiguous_axis is outside plan.shape")
+        if not isinstance(iteration, IterationPlan):
+            raise CpuVectorValidationError("plan.iteration must be an IterationPlan")
         if iteration.element_count != count or iteration.lane_count != lanes:
             raise CpuVectorValidationError("plan iteration does not match element_count/lane_count")
+        if iteration.contiguous_axis != contiguous_axis:
+            raise CpuVectorValidationError("plan iteration contiguous_axis does not match plan")
         if not isinstance(required_features, CpuFeatureSet):
             raise CpuVectorValidationError("required_features must be CpuFeatureSet")
         object.__setattr__(self, "plan_version", plan_version)
         object.__setattr__(self, "operation", operation.strip())
         object.__setattr__(self, "plan_kind", plan_kind)
         object.__setattr__(self, "operation_index", operation_index)
-        object.__setattr__(self, "input_names", tuple(input_names))
-        object.__setattr__(self, "output_name", output_name)
+        object.__setattr__(self, "input_names", normalized_inputs)
+        object.__setattr__(self, "output_name", normalized_output)
         object.__setattr__(self, "dtype", normalized_dtype)
         object.__setattr__(self, "shape", normalized_shape)
         object.__setattr__(self, "layout", layout)
@@ -943,10 +989,49 @@ class CpuVectorProgram:
         policy = _tail_policy(tail_policy)
         if plan_version != VECTOR_PLAN_VERSION:
             raise CpuVectorValidationError("unsupported vector plan version {!r}".format(plan_version))
+        validate_cpu_vector_target(target)
+        try:
+            validate_program(program)
+        except CpuScalarUnsupportedError as exc:
+            raise CpuVectorUnsupportedError("scalar validation rejected vector program: {}".format(exc)) from exc
+        except CpuScalarError as exc:
+            raise CpuVectorValidationError("scalar validation failed: {}".format(exc)) from exc
+        if any(function.body.blocks for function in program.functions):
+            raise CpuVectorUnsupportedError("vector common supports direct operation bodies only")
+        functions = {function.name: function for function in program.functions}
+        if entrypoint not in functions:
+            raise CpuVectorValidationError(
+                "vector entrypoint {!r} is not a program function".format(entrypoint)
+            )
+        function = functions[entrypoint]
+        if len(plan_values) != len(function.body.operations):
+            raise CpuVectorValidationError(
+                "vector plan count {} does not match entrypoint operation count {}".format(
+                    len(plan_values), len(function.body.operations)
+                )
+            )
         if any(plan.lane_count != lanes for plan in plan_values):
             raise CpuVectorValidationError("all vector plans must use the program lane_count")
         if any(plan.required_features != required_features for plan in plan_values):
             raise CpuVectorValidationError("all vector plans must use program required_features")
+        normalized_specialization = frozen_mapping(specialization)
+        expected_plans = tuple(
+            _make_plan_for_operation(
+                program,
+                operation,
+                index,
+                lanes,
+                policy,
+                required_features,
+                normalized_specialization,
+            )
+            for index, operation in enumerate(function.body.operations)
+        )
+        for index, (plan, expected) in enumerate(zip(plan_values, expected_plans)):
+            if plan.to_dict() != expected.to_dict():
+                raise CpuVectorValidationError(
+                    "vector plan at operation index {} does not match its Core Operation".format(index)
+                )
         object.__setattr__(self, "program", program)
         object.__setattr__(self, "target", target)
         object.__setattr__(self, "entrypoint", entrypoint.strip())
@@ -954,7 +1039,7 @@ class CpuVectorProgram:
         object.__setattr__(self, "required_features", required_features)
         object.__setattr__(self, "lane_count", lanes)
         object.__setattr__(self, "tail_policy", policy)
-        object.__setattr__(self, "specialization", frozen_mapping(specialization))
+        object.__setattr__(self, "specialization", normalized_specialization)
         object.__setattr__(self, "plan_version", plan_version)
 
     @property
@@ -969,6 +1054,22 @@ class CpuVectorProgram:
     def features(self) -> CpuFeatureSet:
         return self.required_features
 
+    def validate(self) -> "CpuVectorProgram":
+        """Revalidate a possibly externally tampered frozen value object."""
+
+        CpuVectorProgram(
+            self.program,
+            self.target,
+            self.entrypoint,
+            self.plans,
+            self.required_features,
+            self.lane_count,
+            self.tail_policy,
+            mapping_copy(self.specialization),
+            self.plan_version,
+        )
+        return self
+
     def to_dict(self) -> Mapping[str, Any]:
         return {
             "plan_version": self.plan_version,
diff --git a/python/tests/ut/pypto_x/test_cpu_vector_common.py b/python/tests/ut/pypto_x/test_cpu_vector_common.py
index 2ee006a65..e24c68b15 100644
--- a/python/tests/ut/pypto_x/test_cpu_vector_common.py
+++ b/python/tests/ut/pypto_x/test_cpu_vector_common.py
@@ -18,11 +18,13 @@ from pypto.backends.cpu.vector import (
 )
 from pypto.compiler.targets.cpu_vector import CpuVectorCompiler
 from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation, Region, ValueRef
+from pypto_x.harness import compare_outputs
 from pypto.lowering.cpu.vector import (
     CpuFeatureSet,
     CpuVectorFeatureMismatchError,
     CpuVectorUnsupportedError,
     CpuVectorValidationError,
+    TailPlan,
     TailPolicy,
     make_iteration_plan,
 )
@@ -47,7 +49,7 @@ def _add_program(shape=(11,)):
     return _program("add", (x, y), (z,), (Operation("add", (x, y), (z,)),))
 
 
-def _run(compiler, artifact, inputs, output, runtime=None):
+def _run(artifact, inputs, output, runtime=None):
     runtime = runtime or CpuVectorRuntime()
     return runtime.launch(
         LaunchRequest(
@@ -81,6 +83,59 @@ def test_iteration_plan_is_immutable_and_stable():
     assert plan.to_dict()["chunks"][-1] == {"start": 8, "length": 3, "is_tail": True}
 
 
+def test_hand_constructed_tail_and_vector_plan_fields_are_rejected():
+    with pytest.raises(CpuVectorValidationError, match="TailPolicy"):
+        TailPlan("masked", 0, 1)
+    compiler = CpuVectorCompiler()
+    target_ir = compiler.lower(_add_program((11,)), TARGET)
+    plan = target_ir.plans[0]
+    object.__setattr__(plan, "shape", (12,))
+    with pytest.raises(CpuVectorValidationError, match="does not match"):
+        compiler.compile(target_ir)
+
+
+def test_hand_constructed_plan_order_and_ssa_names_are_revalidated():
+    value_type = CoreType.tensor("float32", (5,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    z = ValueRef("z", value_type)
+    w = ValueRef("w", value_type)
+    program = _program(
+        "two_ops",
+        (x, y),
+        (w,),
+        (
+            Operation("add", (x, y), (z,)),
+            Operation("neg", (z,), (w,)),
+        ),
+    )
+    compiler = CpuVectorCompiler()
+    target_ir = compiler.lower(program, TARGET)
+    object.__setattr__(target_ir, "plans", tuple(reversed(target_ir.plans)))
+    with pytest.raises(CpuVectorValidationError, match="does not match"):
+        compiler.compile(target_ir)
+
+    target_ir = compiler.lower(program, TARGET)
+    object.__setattr__(target_ir.plans[0], "input_names", ("y", "x"))
+    with pytest.raises(CpuVectorValidationError, match="does not match"):
+        compiler.compile(target_ir)
+
+    target_ir = compiler.lower(program, TARGET)
+    object.__setattr__(target_ir.plans[0], "lane_count", 8)
+    with pytest.raises(CpuVectorValidationError, match="lane_count"):
+        compiler.compile(target_ir)
+
+    target_ir = compiler.lower(program, TARGET)
+    object.__setattr__(target_ir.plans[0], "required_features", CpuFeatureSet(("fake",)))
+    with pytest.raises(CpuVectorValidationError, match="required_features"):
+        compiler.compile(target_ir)
+
+    target_ir = compiler.lower(program, TARGET)
+    object.__setattr__(target_ir.plans[0].iteration.tail, "policy", TailPolicy.MASKED)
+    with pytest.raises(CpuVectorValidationError, match="does not match"):
+        compiler.compile(target_ir)
+
+
 def test_elementwise_vector_executor_differentials_scalar_on_tail():
     program = _add_program()
     vector_compiler = CpuVectorCompiler()
@@ -91,7 +146,7 @@ def test_elementwise_vector_executor_differentials_scalar_on_tail():
     right = HostTensor("float32", (11,), [1] * 11)
     vector_output = HostTensor.zeros("float32", (11,))
     scalar_output = HostTensor.zeros("float32", (11,))
-    _run(vector_compiler, vector_artifact, [left, right], vector_output)
+    _run(vector_artifact, [left, right], vector_output)
     CpuScalarRuntime().launch(
         LaunchRequest(
             scalar_artifact,
@@ -100,6 +155,10 @@ def test_elementwise_vector_executor_differentials_scalar_on_tail():
         )
     )
     assert vector_output.flat() == scalar_output.flat()
+    report = compare_outputs(scalar_output.tolist(), vector_output.tolist())
+    assert report.passed
+    assert report.to_dict()["passed"] is True
+    assert report.to_dict()["mismatch_count"] == 0
     plan = vector_compiler.lower(program, TARGET).plans[0]
     assert plan.chunks[-1].start == 8
     assert plan.tail.elements == 3
@@ -120,7 +179,7 @@ def test_reduce_plan_and_executor_differential():
     artifact = vector_compiler.compile(vector_compiler.lower(program, TARGET))
     source = HostTensor("float32", (2, 3, 5), list(range(30)))
     result = HostTensor.zeros("float32", (2, 5))
-    _run(vector_compiler, artifact, [source], result)
+    _run(artifact, [source], result)
     assert result.tolist() == [[15.0, 18.0, 21.0, 24.0, 27.0], [60.0, 63.0, 66.0, 69.0, 72.0]]
     plan = vector_compiler.lower(program, TARGET).plans[0]
     assert plan.plan_kind == "reduction"
@@ -140,7 +199,6 @@ def test_rectangular_matmul_plan_and_executor():
     artifact = compiler.compile(compiler.lower(program, TARGET))
     destination = HostTensor.zeros("float32", (2, 4))
     _run(
-        compiler,
         artifact,
         [
             HostTensor("float32", (2, 3), [[1, 2, 3], [4, 5, 6]]),
@@ -159,7 +217,7 @@ def test_zero_length_vector_executes_without_touching_tail():
     compiler = CpuVectorCompiler()
     artifact = compiler.compile(compiler.lower(program, TARGET))
     output = HostTensor.zeros("float32", (0,))
-    _run(compiler, artifact, [HostTensor("float32", (0,)), HostTensor("float32", (0,))], output)
+    _run(artifact, [HostTensor("float32", (0,)), HostTensor("float32", (0,))], output)
     assert output.flat() == ()
 
 
@@ -187,6 +245,22 @@ def test_artifact_is_deterministic_and_metadata_carries_plan_identity():
     assert first.metadata["layouts"] == ("row_major_contiguous",)
     assert first.metadata["lane_count"] == 4
     assert first.metadata["specialization"] == {}
+    variant_target = TargetSpec(
+        "cpu",
+        "x86_64-linux-gnu",
+        features=("test-vector",),
+        attributes={
+            "lane_count": 8,
+            "tail_policy": "masked",
+            "specialization": {"unroll": 2},
+        },
+    )
+    variant = compiler.compile(compiler.lower(program, variant_target))
+    assert variant.cache_key != first.cache_key
+    assert variant.metadata["required_features"] == ("test-vector",)
+    assert variant.metadata["lane_count"] == 8
+    assert variant.metadata["tail_policy"] == "masked"
+    assert variant.metadata["specialization"] == {"unroll": 2}
 
 
 def test_artifact_plan_tampering_is_rejected():
