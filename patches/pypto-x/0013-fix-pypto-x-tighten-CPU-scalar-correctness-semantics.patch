From d7b7808e9cce311be40aedb71c0726907f00e4e6 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Mon, 7 Sep 2026 02:08:04 +0800
Subject: [PATCH 013/141] fix(pypto-x): tighten CPU scalar correctness
 semantics

---
 python/pypto/backends/cpu/runtime.py       |  35 ++++++--
 python/pypto/compiler/targets/cpu.py       |   6 +-
 python/pypto/lowering/cpu/__init__.py      |  35 +++++++-
 python/tests/ut/pypto_x/test_cpu_scalar.py | 233 ++++++++++++++++++++++++++++++++++++++++++++++++-
 4 files changed, 293 insertions(+), 16 deletions(-)

diff --git a/python/pypto/backends/cpu/runtime.py b/python/pypto/backends/cpu/runtime.py
index 937636e16..a2a02f447 100644
--- a/python/pypto/backends/cpu/runtime.py
+++ b/python/pypto/backends/cpu/runtime.py
@@ -4,13 +4,12 @@ from __future__ import annotations
 
 import hashlib
 import json
-import math
 from dataclasses import dataclass
-from typing import Any, Callable, Dict, List, Mapping, Optional, Sequence, Tuple
+from typing import Any, Callable, Dict, List, Mapping, Sequence, Tuple
 
 from pypto.abi import Availability, LaunchRequest, TensorDesc
 from pypto.compiler.artifact import Artifact
-from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation, ValueRef
+from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation
 from pypto.lowering.cpu import (
     CPU_SCALAR_BACKEND,
     CpuScalarProgram,
@@ -19,7 +18,6 @@ from pypto.lowering.cpu import (
     element_count,
     flatten_values,
     lower_cpu_scalar,
-    reduced_shape,
     static_shape,
 )
 from pypto.target import TargetSpec
@@ -249,14 +247,25 @@ def _is_scalar(value_type: CoreType) -> bool:
 
 
 def _read_tensor(descriptor: TensorDesc, value_type: CoreType, context: str) -> List[Any]:
-    expected_dtype = canonical_dtype(value_type.dtype)
+    try:
+        expected_dtype = canonical_dtype(value_type.dtype)
+        descriptor_dtype = canonical_dtype(descriptor.dtype)
+    except (TypeError, ValueError) as exc:
+        raise CpuScalarExecutionError("{} has an unsupported dtype: {}".format(context, exc)) from exc
     expected_shape = static_shape(value_type, context)
-    if descriptor.dtype != expected_dtype or descriptor.shape != expected_shape:
+    if descriptor_dtype != expected_dtype or descriptor.shape != expected_shape:
         raise CpuScalarExecutionError(
             "{} descriptor dtype/shape ({}, {}) does not match expected ({}, {})".format(
                 context, descriptor.dtype, descriptor.shape, expected_dtype, expected_shape
             )
         )
+    if isinstance(descriptor.storage, HostTensor):
+        try:
+            storage_dtype = canonical_dtype(descriptor.storage.dtype)
+        except (TypeError, ValueError) as exc:
+            raise CpuScalarExecutionError("{} HostTensor has an unsupported dtype: {}".format(context, exc)) from exc
+        if storage_dtype != expected_dtype or descriptor.storage.shape != expected_shape:
+            raise CpuScalarExecutionError("{} HostTensor metadata does not match input descriptor".format(context))
     values = _storage_values(descriptor.storage, context)
     expected_count = element_count(expected_shape)
     if len(values) != expected_count:
@@ -270,9 +279,13 @@ def _read_tensor(descriptor: TensorDesc, value_type: CoreType, context: str) ->
 
 
 def _write_tensor(descriptor: TensorDesc, value_type: CoreType, values: Sequence[Any], context: str) -> None:
-    expected_dtype = canonical_dtype(value_type.dtype)
+    try:
+        expected_dtype = canonical_dtype(value_type.dtype)
+        descriptor_dtype = canonical_dtype(descriptor.dtype)
+    except (TypeError, ValueError) as exc:
+        raise CpuScalarExecutionError("{} has an unsupported dtype: {}".format(context, exc)) from exc
     expected_shape = static_shape(value_type, context)
-    if descriptor.dtype != expected_dtype or descriptor.shape != expected_shape:
+    if descriptor_dtype != expected_dtype or descriptor.shape != expected_shape:
         raise CpuScalarExecutionError(
             "{} output descriptor dtype/shape ({}, {}) does not match expected ({}, {})".format(
                 context, descriptor.dtype, descriptor.shape, expected_dtype, expected_shape
@@ -280,7 +293,11 @@ def _write_tensor(descriptor: TensorDesc, value_type: CoreType, values: Sequence
         )
     storage = descriptor.storage
     if isinstance(storage, HostTensor):
-        if storage.dtype != expected_dtype or storage.shape != expected_shape:
+        try:
+            storage_dtype = canonical_dtype(storage.dtype)
+        except (TypeError, ValueError) as exc:
+            raise CpuScalarExecutionError("{} HostTensor has an unsupported dtype: {}".format(context, exc)) from exc
+        if storage_dtype != expected_dtype or storage.shape != expected_shape:
             raise CpuScalarExecutionError("{} HostTensor metadata does not match output descriptor".format(context))
         destination = storage.data
     elif isinstance(storage, FlatBuffer):
diff --git a/python/pypto/compiler/targets/cpu.py b/python/pypto/compiler/targets/cpu.py
index f083dd802..61783b847 100644
--- a/python/pypto/compiler/targets/cpu.py
+++ b/python/pypto/compiler/targets/cpu.py
@@ -87,7 +87,7 @@ class CpuScalarCompiler:
         else:
             artifact_format = DEFAULT_ARTIFACT_FORMAT
             abi_version = ABI_VERSION
-        return Artifact(
+        artifact = Artifact(
             format=artifact_format,
             entrypoint=target_ir.entrypoint,
             target=target_ir.target,
@@ -95,6 +95,10 @@ class CpuScalarCompiler:
             payload=payload,
             metadata=metadata,
         )
+        # Direct callers of this target compiler receive the same validation
+        # guarantee as callers going through ``compile_program``.
+        artifact.validate()
+        return artifact
 
 
 def _plain_mapping(value: Mapping[str, Any]) -> dict:
diff --git a/python/pypto/lowering/cpu/__init__.py b/python/pypto/lowering/cpu/__init__.py
index f6b32da45..99256b560 100644
--- a/python/pypto/lowering/cpu/__init__.py
+++ b/python/pypto/lowering/cpu/__init__.py
@@ -9,9 +9,9 @@ third-party numerical library.
 from __future__ import annotations
 
 from dataclasses import dataclass
-from typing import Any, Iterable, Mapping, Optional, Sequence, Tuple
+from typing import Any, Mapping, Optional, Sequence, Tuple
 
-from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation, Region
+from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Effect, Operation, Region
 from pypto.target import TargetSpec
 
 
@@ -97,6 +97,7 @@ OP_ALIASES = {
     "linalg.matmul": "matmul",
 }
 SUPPORTED_OPERATIONS = frozenset(OP_ALIASES)
+DIVISION_DTYPES = frozenset({"float16", "bf16", "float32", "float64"})
 
 
 def canonical_operation_name(name: str) -> str:
@@ -267,6 +268,12 @@ def element_count(shape: Sequence[int]) -> int:
 
 def _validate_operation(operation: Operation, context: str) -> None:
     name = canonical_operation_name(operation.name)
+    if operation.effect is not Effect.PURE:
+        raise CpuScalarUnsupportedError(
+            "{} operation {!r} requires Effect.PURE, got {}".format(
+                context, operation.name, operation.effect.value
+            )
+        )
     if operation.regions:
         raise CpuScalarUnsupportedError("{} contains regions; structured control flow is not in W2".format(context))
     inputs = operation.inputs
@@ -298,6 +305,18 @@ def _validate_operation(operation: Operation, context: str) -> None:
             if not (_is_tensor(input_ref.type) or _is_scalar(input_ref.type)):
                 raise CpuScalarUnsupportedError("{} input kind {!r} is unsupported".format(context, input_ref.type.kind))
         _validate_dtype(output_type, context + ".output")
+        dtypes = [canonical_dtype(ref.type.dtype) for ref in inputs]
+        dtypes.append(canonical_dtype(output_type.dtype))
+        if "bool" in dtypes:
+            raise CpuScalarUnsupportedError(
+                "{} {} does not support bool; bool is limited to constant and identity".format(context, name)
+            )
+        if name == "div" and any(dtype not in DIVISION_DTYPES for dtype in dtypes):
+            raise CpuScalarUnsupportedError(
+                "{} div supports only float16, bf16, float32, and float64; integer/bool division is undefined".format(
+                    context
+                )
+            )
         if _is_tensor(inputs[0].type) and _is_tensor(inputs[1].type):
             _same_type(inputs[0].type, inputs[1].type, context + ".binary")
             _same_type(inputs[0].type, output_type, context + ".binary output")
@@ -320,6 +339,10 @@ def _validate_operation(operation: Operation, context: str) -> None:
         output_type = _one_output(operation, name)
         _same_type(inputs[0].type, output_type, context + ".neg")
         _validate_dtype(output_type, context + ".output")
+        if canonical_dtype(output_type.dtype) == "bool":
+            raise CpuScalarUnsupportedError(
+                "{} neg does not support bool; bool is limited to constant and identity".format(context)
+            )
         if not (_is_tensor(output_type) or _is_scalar(output_type)):
             raise CpuScalarUnsupportedError("{} neg kind {!r} is unsupported".format(context, output_type.kind))
         if _is_tensor(output_type):
@@ -334,6 +357,10 @@ def _validate_operation(operation: Operation, context: str) -> None:
             raise CpuScalarUnsupportedError("{} reduction only supports tensor values".format(context))
         _validate_dtype(input_type, context + ".input")
         _validate_dtype(output_type, context + ".output")
+        if canonical_dtype(input_type.dtype) == "bool" or canonical_dtype(output_type.dtype) == "bool":
+            raise CpuScalarUnsupportedError(
+                "{} {} does not support bool; bool is limited to constant and identity".format(context, name)
+            )
         _same_dtype(input_type, output_type, context + ".reduction dtype")
         input_shape = static_shape(input_type, context + ".input")
         output_shape = static_shape(output_type, context + ".output")
@@ -354,6 +381,10 @@ def _validate_operation(operation: Operation, context: str) -> None:
             if len(ref.type.shape) != 2:
                 raise CpuScalarValidationError("{} matmul inputs must be rank 2".format(context))
         _validate_dtype(output_type, context + ".output")
+        if any(canonical_dtype(ref.type.dtype) == "bool" for ref in inputs) or canonical_dtype(output_type.dtype) == "bool":
+            raise CpuScalarUnsupportedError(
+                "{} matmul does not support bool; bool is limited to constant and identity".format(context)
+            )
         _same_dtype(inputs[0].type, inputs[1].type, context + ".matmul dtype")
         _same_dtype(inputs[0].type, output_type, context + ".matmul output")
         left_shape = static_shape(inputs[0].type, context + ".left")
diff --git a/python/tests/ut/pypto_x/test_cpu_scalar.py b/python/tests/ut/pypto_x/test_cpu_scalar.py
index 3b1a01877..6327846a6 100644
--- a/python/tests/ut/pypto_x/test_cpu_scalar.py
+++ b/python/tests/ut/pypto_x/test_cpu_scalar.py
@@ -11,6 +11,7 @@ import types
 from pathlib import Path
 
 import pytest
+from pypto_x.harness import compare_outputs
 
 
 def _imports():
@@ -22,12 +23,14 @@ def _imports():
     package.__path__ = [str(package_root)]
     sys.modules["pypto"] = package
     try:
-        from pypto.abi import LaunchRequest
+        from pypto.abi import LaunchRequest, TensorDesc
         from pypto.backends.cpu import CpuScalarRuntime, HostTensor
+        from pypto.compiler import CompileOptions
         from pypto.compiler.targets.cpu import CpuScalarCompiler
-        from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation, Region, ValueRef
+        from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Effect, Operation, Region, ValueRef
         from pypto.lowering.cpu import CpuScalarUnsupportedError
-        from pypto.target import TargetSpec
+        from pypto.backends.cpu import CpuScalarExecutionError
+        from pypto.target import TargetSpec, VersionMismatchError
         return locals()
     finally:
         for name in list(sys.modules):
@@ -39,6 +42,7 @@ def _imports():
 # Keep imports in one place to avoid invoking the native upstream initializer.
 _C = _imports()
 LaunchRequest = _C["LaunchRequest"]
+TensorDesc = _C["TensorDesc"]
 CpuScalarRuntime = _C["CpuScalarRuntime"]
 HostTensor = _C["HostTensor"]
 CpuScalarCompiler = _C["CpuScalarCompiler"]
@@ -46,10 +50,14 @@ CoreFunction = _C["CoreFunction"]
 CoreProgram = _C["CoreProgram"]
 CoreType = _C["CoreType"]
 Operation = _C["Operation"]
+Effect = _C["Effect"]
 Region = _C["Region"]
 ValueRef = _C["ValueRef"]
 CpuScalarUnsupportedError = _C["CpuScalarUnsupportedError"]
+CpuScalarExecutionError = _C["CpuScalarExecutionError"]
+CompileOptions = _C["CompileOptions"]
 TargetSpec = _C["TargetSpec"]
+VersionMismatchError = _C["VersionMismatchError"]
 
 
 TARGET = TargetSpec("cpu", "x86_64-linux-gnu")
@@ -84,6 +92,105 @@ def test_add_uses_common_launch_abi_and_is_deterministic():
     result = CpuScalarRuntime().launch(request)
     assert result == (output,)
     assert output.tolist() == [[11.0, 22.0, 33.0], [44.0, 55.0, 66.0]]
+    report = compare_outputs(output.tolist(), [[11.0, 22.0, 33.0], [44.0, 55.0, 66.0]])
+    assert report.passed
+    assert report.to_dict()["mismatch_count"] == 0
+
+
+def test_constant_then_neg_executes():
+    value_type = CoreType.tensor("float32", (2,))
+    constant = ValueRef("constant", value_type)
+    output = ValueRef("output", value_type)
+    program = _program(
+        "constant_neg",
+        (),
+        (output,),
+        (
+            Operation("constant", outputs=(constant,), attributes={"values": [1, 2]}),
+            Operation("neg", inputs=(constant,), outputs=(output,)),
+        ),
+    )
+    _, artifact = _compile(program)
+    destination = HostTensor.zeros("float32", (2,))
+    CpuScalarRuntime().launch(LaunchRequest(artifact, outputs=[destination.descriptor()]))
+    assert destination.tolist() == [-1.0, -2.0]
+
+
+def test_bool_constant_and_identity_remain_supported():
+    value_type = CoreType.tensor("bool", (2,))
+    constant = ValueRef("constant", value_type)
+    output = ValueRef("output", value_type)
+    program = _program(
+        "bool_identity",
+        (),
+        (output,),
+        (
+            Operation("constant", outputs=(constant,), attributes={"values": [True, False]}),
+            Operation("identity", inputs=(constant,), outputs=(output,)),
+        ),
+    )
+    _, artifact = _compile(program)
+    destination = HostTensor.zeros("bool", (2,))
+    CpuScalarRuntime().launch(LaunchRequest(artifact, outputs=[destination.descriptor()]))
+    assert destination.tolist() == [True, False]
+
+
+@pytest.mark.parametrize(
+    ("operation_name", "expected"),
+    (("sub", [4.0, 5.0]), ("mul", [5.0, 14.0])),
+)
+def test_sub_and_mul_execute(operation_name, expected):
+    value_type = CoreType.tensor("float32", (2,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    output = ValueRef("output", value_type)
+    program = _program(operation_name, (x, y), (output,), (Operation(operation_name, (x, y), (output,)),))
+    _, artifact = _compile(program)
+    destination = HostTensor.zeros("float32", (2,))
+    CpuScalarRuntime().launch(
+        LaunchRequest(
+            artifact,
+            inputs=[
+                HostTensor("float32", (2,), [5, 7]).descriptor(readonly=True),
+                HostTensor("float32", (2,), [1, 2]).descriptor(readonly=True),
+            ],
+            outputs=[destination.descriptor()],
+        )
+    )
+    assert destination.tolist() == expected
+
+
+def test_float_div_executes_and_zero_divisor_fails():
+    value_type = CoreType.tensor("float32", (2,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    output = ValueRef("output", value_type)
+    program = _program("div", (x, y), (output,), (Operation("div", (x, y), (output,)),))
+    _, artifact = _compile(program)
+    destination = HostTensor.zeros("float32", (2,))
+    runtime = CpuScalarRuntime()
+    runtime.launch(
+        LaunchRequest(
+            artifact,
+            inputs=[
+                HostTensor("float32", (2,), [4, 9]).descriptor(readonly=True),
+                HostTensor("float32", (2,), [2, 3]).descriptor(readonly=True),
+            ],
+            outputs=[destination.descriptor()],
+        )
+    )
+    assert destination.tolist() == [2.0, 3.0]
+    with pytest.raises(CpuScalarExecutionError, match="division by zero"):
+        runtime.launch(
+            LaunchRequest(
+                artifact,
+                inputs=[
+                    HostTensor("float32", (2,), [4, 9]).descriptor(readonly=True),
+                    HostTensor("float32", (2,), [0, 3]).descriptor(readonly=True),
+                ],
+                outputs=[destination.descriptor()],
+            )
+        )
 
 
 def test_reduce_sum_supports_non_divisible_shape():
@@ -104,6 +211,24 @@ def test_reduce_sum_supports_non_divisible_shape():
     assert output.tolist() == [[15, 18, 21, 24, 27], [60, 63, 66, 69, 72]]
 
 
+def test_reduce_max_executes():
+    input_type = CoreType.tensor("float32", (2, 3))
+    output_type = CoreType.tensor("float32", (2,))
+    x = ValueRef("x", input_type)
+    y = ValueRef("y", output_type)
+    program = _program(
+        "reduce_max",
+        (x,),
+        (y,),
+        (Operation("reduce_max", (x,), (y,), attributes={"axis": 1}),),
+    )
+    _, artifact = _compile(program)
+    source = HostTensor("float32", (2, 3), [[1, 9, 3], [8, 2, 7]])
+    output = HostTensor.zeros("float32", (2,))
+    CpuScalarRuntime().launch(LaunchRequest(artifact, inputs=[source.descriptor(readonly=True)], outputs=[output.descriptor()]))
+    assert output.tolist() == [9.0, 8.0]
+
+
 def test_rectangular_matmul():
     left_type = CoreType.tensor("float32", (2, 3))
     right_type = CoreType.tensor("float32", (3, 4))
@@ -124,7 +249,6 @@ def test_rectangular_matmul():
 
 def test_shape_mismatch_and_unknown_operation_fail_explicitly():
     value_type = CoreType.tensor("float32", (2, 3))
-    wrong_type = CoreType.tensor("float32", (2, 2))
     x = ValueRef("x", value_type)
     y = ValueRef("y", value_type)
     z = ValueRef("z", value_type)
@@ -142,6 +266,107 @@ def test_shape_mismatch_and_unknown_operation_fail_explicitly():
         CpuScalarCompiler().lower(unknown, TARGET)
 
 
+@pytest.mark.parametrize("operation_name", ("add", "sub", "mul", "div", "neg"))
+def test_bool_arithmetic_is_explicitly_unsupported(operation_name):
+    value_type = CoreType.tensor("bool", (2,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    output = ValueRef("output", value_type)
+    inputs = (x,) if operation_name == "neg" else (x, y)
+    program = _program(operation_name, inputs, (output,), (Operation(operation_name, inputs, (output,)),))
+    with pytest.raises(CpuScalarUnsupportedError, match="bool"):
+        CpuScalarCompiler().lower(program, TARGET)
+
+
+def test_bool_reduce_and_matmul_are_unsupported():
+    matrix_type = CoreType.tensor("bool", (2, 2))
+    reduced_type = CoreType.tensor("bool", ())
+    x = ValueRef("x", matrix_type)
+    reduced = ValueRef("reduced", reduced_type)
+    reduce_program = _program(
+        "bool_reduce",
+        (x,),
+        (reduced,),
+        (Operation("reduce_max", (x,), (reduced,)),),
+    )
+    with pytest.raises(CpuScalarUnsupportedError, match="bool"):
+        CpuScalarCompiler().lower(reduce_program, TARGET)
+
+    right = ValueRef("right", matrix_type)
+    output = ValueRef("output", matrix_type)
+    matmul_program = _program(
+        "bool_matmul",
+        (x, right),
+        (output,),
+        (Operation("matmul", (x, right), (output,)),),
+    )
+    with pytest.raises(CpuScalarUnsupportedError, match="bool"):
+        CpuScalarCompiler().lower(matmul_program, TARGET)
+
+
+def test_integer_division_is_unsupported_instead_of_truncating():
+    value_type = CoreType.tensor("int32", (2,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    output = ValueRef("output", value_type)
+    program = _program("int_div", (x, y), (output,), (Operation("div", (x, y), (output,)),))
+    with pytest.raises(CpuScalarUnsupportedError, match="only float16"):
+        CpuScalarCompiler().lower(program, TARGET)
+
+
+@pytest.mark.parametrize("effect", (Effect.READ, Effect.WRITE, Effect.READ_WRITE, Effect.BARRIER))
+def test_non_pure_effect_is_rejected(effect):
+    value_type = CoreType.tensor("float32", (2,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    output = ValueRef("output", value_type)
+    program = _program(
+        "effectful_add",
+        (x, y),
+        (output,),
+        (Operation("add", (x, y), (output,), effect=effect),),
+    )
+    with pytest.raises(CpuScalarUnsupportedError, match="Effect.PURE"):
+        CpuScalarCompiler().lower(program, TARGET)
+
+
+def test_dtype_aliases_match_core_and_runtime_descriptors():
+    value_type = CoreType.tensor("bfloat16", (2,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    output = ValueRef("output", value_type)
+    program = _program("bf16_add", (x, y), (output,), (Operation("add", (x, y), (output,)),))
+    _, artifact = _compile(program)
+    left = HostTensor("bf16", (2,), [1, 2])
+    right = HostTensor("bf16", (2,), [3, 4])
+    destination = HostTensor.zeros("bf16", (2,))
+    # Exercise both TensorDesc aliases and HostTensor metadata normalization.
+    left.dtype = "bfloat16"
+    right.dtype = "bfloat16"
+    destination.dtype = "bfloat16"
+    CpuScalarRuntime().launch(
+        LaunchRequest(
+            artifact,
+            inputs=[
+                TensorDesc("bfloat16", (2,), handle=left, readonly=True),
+                TensorDesc("bfloat16", (2,), handle=right, readonly=True),
+            ],
+            outputs=[TensorDesc("bfloat16", (2,), handle=destination)],
+        )
+    )
+    assert destination.tolist() == [4.0, 6.0]
+
+
+def test_direct_compiler_validates_artifact_before_returning():
+    value_type = CoreType.tensor("float32", (1,))
+    x = ValueRef("x", value_type)
+    y = ValueRef("y", value_type)
+    program = _program("copy", (x,), (y,), (Operation("identity", (x,), (y,)),))
+    compiler = CpuScalarCompiler()
+    with pytest.raises(VersionMismatchError):
+        compiler.compile(compiler.lower(program, TARGET), CompileOptions(abi_version="2"))
+
+
 def test_artifact_payload_tampering_is_detected():
     value_type = CoreType.tensor("float32", (1,))
     x = ValueRef("x", value_type)
