From 47c44ab4d858c4aff63015875959f31fc39e6ab5 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Mon, 7 Sep 2026 04:37:01 +0800
Subject: [PATCH 027/144] fix(pypto-x): finalize AVX2 freeze audit

---
 python/pypto/backends/cpu/x86/avx2.py              |   7 ++
 python/pypto/compiler/targets/cpu_avx2.py          |  86 +++++++++++--------
 python/pypto/lowering/cpu/x86/avx2/capabilities.py |  31 ++++---
 python/tests/ut/pypto_x/test_cpu_avx2.py           | 186 ++++++++++++++++++++++++++++++++++++-----
 4 files changed, 242 insertions(+), 68 deletions(-)

diff --git a/python/pypto/backends/cpu/x86/avx2.py b/python/pypto/backends/cpu/x86/avx2.py
index 683b13f0a..53e1682f7 100644
--- a/python/pypto/backends/cpu/x86/avx2.py
+++ b/python/pypto/backends/cpu/x86/avx2.py
@@ -180,6 +180,13 @@ def _array_interface_buffer(
     except ValueError as exc:
         raise CpuAvx2ExecutionError("{} array interface itemsize is invalid".format(context)) from exc
     kind = typestr[1]
+    byteorder = typestr[0]
+    if byteorder not in ("<", "=", "|"):
+        raise CpuAvx2ExecutionError(
+            "{} array interface byte order {!r} is not native little-endian".format(
+                context, byteorder
+            )
+        )
     if expected_dtype == "float32":
         dtype_match = kind == "f" and itemsize == 4
     else:
diff --git a/python/pypto/compiler/targets/cpu_avx2.py b/python/pypto/compiler/targets/cpu_avx2.py
index 79cccb97d..7e24f98e0 100644
--- a/python/pypto/compiler/targets/cpu_avx2.py
+++ b/python/pypto/compiler/targets/cpu_avx2.py
@@ -373,7 +373,12 @@ def _compiler_info(compiler_path: str) -> tuple:
         universal_newlines=True,
         check=False,
     )
-    return compiler_version, triple_result.stdout.strip() or "unknown"
+    target_triple = triple_result.stdout.strip() or "unknown"
+    if not target_triple.lower().startswith(("x86_64", "amd64")):
+        raise Avx2CompileError(
+            "Clang target triple {!r} is not an x86_64/amd64 target".format(target_triple)
+        )
+    return compiler_version, target_triple
 
 
 def _compile_native(target_ir: Avx2Program, options: Optional[CompileOptions]) -> tuple:
@@ -392,38 +397,43 @@ def _compile_native(target_ir: Avx2Program, options: Optional[CompileOptions]) -
     stem = "pypto_x_avx2_{}".format(source_digest[:20])
     temporary_dir = build_dir / "tmp"
     temporary_dir.mkdir(parents=True, exist_ok=True)
-    source_path = temporary_dir / (stem + ".c")
-    temporary_output = temporary_dir / (stem + ".so")
-    source_path.write_text(source, encoding="utf-8")
-    command = [
-        compiler_path,
-        "-shared",
-        "-fPIC",
-        "-O2",
-        "-std=c99",
-        "-mavx2",
-    ]
-    if target_ir.use_fma:
-        command.append("-mfma")
-    command.extend([str(source_path), "-o", str(temporary_output)])
-    environment = dict(os.environ)
-    environment["TMPDIR"] = str(build_dir / "tmp")
-    result = subprocess.run(
-        command,
-        cwd=str(build_dir),
-        env=environment,
-        stdout=subprocess.PIPE,
-        stderr=subprocess.PIPE,
-        universal_newlines=True,
-        check=False,
-    )
-    if result.returncode != 0 or not temporary_output.is_file():
-        raise Avx2CompileError(
-            "Clang AVX2 compilation failed ({}): {}".format(
-                result.returncode, (result.stderr or result.stdout).strip()
-            )
+    invocation_dir = Path(tempfile.mkdtemp(prefix=stem + "-", dir=str(temporary_dir)))
+    source_path = invocation_dir / (stem + ".c")
+    temporary_output = invocation_dir / (stem + ".so")
+    source_name = source_path.name
+    try:
+        source_path.write_text(source, encoding="utf-8")
+        command = [
+            compiler_path,
+            "-shared",
+            "-fPIC",
+            "-O2",
+            "-std=c99",
+            "-mavx2",
+        ]
+        if target_ir.use_fma:
+            command.append("-mfma")
+        command.extend([str(source_path), "-o", str(temporary_output)])
+        environment = dict(os.environ)
+        environment["TMPDIR"] = str(build_dir / "tmp")
+        result = subprocess.run(
+            command,
+            cwd=str(invocation_dir),
+            env=environment,
+            stdout=subprocess.PIPE,
+            stderr=subprocess.PIPE,
+            universal_newlines=True,
+            check=False,
         )
-    native_bytes = temporary_output.read_bytes()
+        if result.returncode != 0 or not temporary_output.is_file():
+            raise Avx2CompileError(
+                "Clang AVX2 compilation failed ({}): {}".format(
+                    result.returncode, (result.stderr or result.stdout).strip()
+                )
+            )
+        native_bytes = temporary_output.read_bytes()
+    finally:
+        shutil.rmtree(str(invocation_dir), ignore_errors=True)
     native_digest = sha256_bytes(native_bytes)
     output_path = artifact_dir / ("pypto_x_avx2_{}.so".format(native_digest))
     if output_path.exists():
@@ -449,7 +459,7 @@ def _compile_native(target_ir: Avx2Program, options: Optional[CompileOptions]) -
                 publication_path.unlink()
     normalized_command = [Path(compiler_path).name]
     normalized_command.extend(command[1:-3])
-    normalized_command.extend([source_path.name, "-o", output_path.name])
+    normalized_command.extend([source_name, "-o", output_path.name])
     return (
         output_path,
         source_digest,
@@ -475,6 +485,16 @@ class Avx2Compiler:
             raise TypeError("AVX2 compile() requires Avx2Program from lower()")
         if options is not None and not isinstance(options, CompileOptions):
             raise TypeError("options must be CompileOptions")
+        if options is not None and options.artifact_format != DEFAULT_ARTIFACT_FORMAT:
+            raise Avx2CompileError(
+                "AVX2 compiler requires artifact format {!r}, got {!r}".format(
+                    DEFAULT_ARTIFACT_FORMAT, options.artifact_format
+                )
+            )
+        if options is not None and options.abi_version != ABI_VERSION:
+            raise Avx2CompileError(
+                "AVX2 compiler requires ABI {!r}, got {!r}".format(ABI_VERSION, options.abi_version)
+            )
         target_ir.validate()
         (
             output_path,
diff --git a/python/pypto/lowering/cpu/x86/avx2/capabilities.py b/python/pypto/lowering/cpu/x86/avx2/capabilities.py
index e5012f5c9..248e6fffe 100644
--- a/python/pypto/lowering/cpu/x86/avx2/capabilities.py
+++ b/python/pypto/lowering/cpu/x86/avx2/capabilities.py
@@ -226,18 +226,19 @@ def _probe_directory(directory: Optional[Any]) -> Path:
     return path
 
 
-def _build_probe(compiler: str, directory: Path) -> Path:
-    source_path = directory / "cpuid_probe.c"
-    output_path = directory / "cpuid_probe.so"
+def _build_probe(compiler: str, directory: Path) -> Tuple[Path, Path]:
+    invocation_directory = Path(tempfile.mkdtemp(prefix="cpuid-probe-", dir=str(directory)))
+    source_path = invocation_directory / "cpuid_probe.c"
+    output_path = invocation_directory / "cpuid_probe.so"
     source_path.write_text(_PROBE_SOURCE, encoding="utf-8")
-    temporary = directory / "tmp"
+    temporary = invocation_directory / "tmp"
     temporary.mkdir(parents=True, exist_ok=True)
     env = dict(os.environ)
     env["TMPDIR"] = str(temporary)
     command = [compiler, "-shared", "-fPIC", "-O2", "-std=c99", str(source_path), "-o", str(output_path)]
     result = subprocess.run(
         command,
-        cwd=str(directory),
+        cwd=str(invocation_directory),
         env=env,
         stdout=subprocess.PIPE,
         stderr=subprocess.PIPE,
@@ -245,10 +246,11 @@ def _build_probe(compiler: str, directory: Path) -> Path:
         check=False,
     )
     if result.returncode != 0 or not output_path.is_file():
+        shutil.rmtree(str(invocation_directory), ignore_errors=True)
         raise Avx2ProbeError(
             "CPUID/XGETBV probe compilation failed: {}".format((result.stderr or result.stdout).strip())
         )
-    return output_path
+    return output_path, invocation_directory
 
 
 def _run_probe(compiler: str, directory: Optional[Any]) -> Avx2Capability:
@@ -256,13 +258,16 @@ def _run_probe(compiler: str, directory: Optional[Any]) -> Avx2Capability:
     if not shutil.which(compiler_path) and not Path(compiler_path).is_file():
         raise Avx2ProbeError("Clang compiler {!r} was not found".format(compiler))
     compiler_text, triple = _tool_info(compiler_path)
-    output_path = _build_probe(compiler_path, _probe_directory(directory))
-    library = ctypes.CDLL(str(output_path))
-    function = library.ptx_probe
-    function.argtypes = [ctypes.POINTER(ctypes.c_uint64)]
-    function.restype = None
-    values = (ctypes.c_uint64 * 10)()
-    function(values)
+    output_path, invocation_directory = _build_probe(compiler_path, _probe_directory(directory))
+    try:
+        library = ctypes.CDLL(str(output_path))
+        function = library.ptx_probe
+        function.argtypes = [ctypes.POINTER(ctypes.c_uint64)]
+        function.restype = None
+        values = (ctypes.c_uint64 * 10)()
+        function(values)
+    finally:
+        shutil.rmtree(str(invocation_directory), ignore_errors=True)
     max_leaf, leaf1_ecx, leaf1_edx, leaf7_ebx, xcr0, osxsave, xgetbv_valid, avx, avx2, fma = _plain_output(values)
     ymm = bool(osxsave and xgetbv_valid and (xcr0 & 0x6) == 0x6)
     features = []
diff --git a/python/tests/ut/pypto_x/test_cpu_avx2.py b/python/tests/ut/pypto_x/test_cpu_avx2.py
index ff9c1a7fd..76290d4de 100644
--- a/python/tests/ut/pypto_x/test_cpu_avx2.py
+++ b/python/tests/ut/pypto_x/test_cpu_avx2.py
@@ -8,9 +8,11 @@ smoke and an objdump report outside the source worktree.
 
 from __future__ import annotations
 
+from concurrent.futures import ThreadPoolExecutor
 import ctypes
 import os
 from pathlib import Path
+import re
 import shutil
 import struct
 import subprocess
@@ -41,6 +43,7 @@ def _imports():
         from pypto.compiler import CompileOptions
         from pypto.compiler.targets.cpu import CpuScalarCompiler
         from pypto.compiler.targets.cpu_avx2 import Avx2CompileError, Avx2Compiler
+        import pypto.compiler.targets.cpu_avx2 as compiler_module
         from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation, Region, ValueRef
         from pypto.lowering.cpu.x86.avx2 import Avx2Capability, Avx2UnsupportedError, lower_avx2
         from pypto.lowering.cpu.x86.avx2 import capabilities as capabilities_module
@@ -80,6 +83,7 @@ lower_avx2 = _C["lower_avx2"]
 TargetSpec = _C["TargetSpec"]
 compare_outputs = _C["compare_outputs"]
 capabilities_module = _C["capabilities_module"]
+compiler_module = _C["compiler_module"]
 
 
 def _program(name, parameters, returns, operations):
@@ -112,6 +116,18 @@ def _bf16_value(bits):
     return struct.unpack("<f", struct.pack("<I", (int(bits) & 0xFFFF) << 16))[0]
 
 
+def _float32(value):
+    return struct.unpack("<f", struct.pack("<f", float(value)))[0]
+
+
+def _float32_add(left, right):
+    return _float32(_float32(left) + _float32(right))
+
+
+def _float32_mul(left, right):
+    return _float32(_float32(left) * _float32(right))
+
+
 def _compile(program, target, tmp_path):
     compiler = Avx2Compiler()
     options = CompileOptions(
@@ -151,6 +167,19 @@ def test_cpuid_xgetbv_probe_has_structured_host_result():
         assert isinstance(runtime.capability.xcr0, int)
 
 
+def test_cpuid_probe_shared_directory_is_race_free(tmp_path):
+    capabilities_module.probe_avx2_capabilities.cache_clear()
+    probe_directory = tmp_path / "shared-probe"
+
+    def probe_once(_index):
+        return capabilities_module.probe_avx2_capabilities(directory=str(probe_directory))
+
+    with ThreadPoolExecutor(max_workers=2) as executor:
+        results = list(executor.map(probe_once, (0, 1)))
+    assert all(result.source == "cpuid+osxsave+xgetbv" for result in results)
+    assert not list(probe_directory.iterdir())
+
+
 def test_avx2_required_features_are_explicit(tmp_path):
     _, target_ir, _ = _compile(_add_program(), _target("avx2"), tmp_path / "base")
     assert target_ir.required_features.features == ("avx", "avx2", "osxsave", "ymm")
@@ -203,7 +232,7 @@ def test_injected_capability_cannot_bypass_real_probe_before_native_launch(tmp_p
 @pytest.mark.parametrize("length", (0, 1, 7, 8, 9, 19))
 def test_elementwise_lengths_differential_against_scalar(length, tmp_path):
     program = _add_program(shape=(length,))
-    target = _target("avx2", "fma")
+    target = _target("avx2")
     compiler, _, artifact = _compile(program, target, tmp_path)
     runtime = _runtime()
     left = HostTensor("float32", (length,), [float(index) for index in range(length)])
@@ -236,7 +265,7 @@ def test_all_fp32_elementwise_operations_have_structured_scalar_differential(ope
     output = ValueRef("output", value_type)
     inputs = (left,) if operation == "neg" else (left, right)
     program = _program(operation, inputs, (output,), (Operation(operation, inputs, (output,)),))
-    _, _, artifact = _compile(program, _target("avx2", "fma"), tmp_path / "avx2")
+    _, _, artifact = _compile(program, _target("avx2"), tmp_path / "avx2")
     left_tensor = HostTensor("float32", (19,), [0.25 + index * 0.125 for index in range(19)])
     right_tensor = HostTensor("float32", (19,), [1.0 + index * 0.03125 for index in range(19)])
     avx_output = HostTensor.zeros("float32", (19,))
@@ -338,6 +367,35 @@ def test_array_interface_validates_2d_dtype_shape_strides_and_readonly(tmp_path)
             )
         )
 
+    big_endian = np.ones((2, 9), dtype=">f4")
+    with pytest.raises(CpuAvx2ExecutionError, match="byte order"):
+        Avx2Runtime().launch(
+            LaunchRequest(
+                artifact,
+                inputs=[
+                    TensorDesc("float32", (2, 9), handle=big_endian, readonly=True),
+                    TensorDesc("float32", (2, 9), handle=right, readonly=True),
+                ],
+                outputs=[TensorDesc("float32", (2, 9), handle=output)],
+            )
+        )
+
+    bf16_program = _add_program(dtype="bf16", shape=(2, 9))
+    _, _, bf16_artifact = _compile(bf16_program, _target("avx2"), tmp_path / "big-endian-bf16")
+    big_endian_u16 = np.ones((2, 9), dtype=">u2")
+    native_u16 = np.ones((2, 9), dtype=np.uint16)
+    with pytest.raises(CpuAvx2ExecutionError, match="byte order"):
+        Avx2Runtime().launch(
+            LaunchRequest(
+                bf16_artifact,
+                inputs=[
+                    TensorDesc("bf16", (2, 9), handle=big_endian_u16, readonly=True),
+                    TensorDesc("bf16", (2, 9), handle=native_u16, readonly=True),
+                ],
+                outputs=[TensorDesc("bf16", (2, 9), handle=np.zeros((2, 9), dtype=np.uint16))],
+            )
+        )
+
     readonly_output = np.zeros((2, 9), dtype=np.float32)
     readonly_output.flags.writeable = False
     with pytest.raises(CpuAvx2ExecutionError, match="read-only"):
@@ -438,7 +496,13 @@ def test_bf16_reduction_and_matmul_match_scalar(tmp_path):
         (Operation("reduce_sum", (source,), (reduced,), attributes={"axis": 1}),),
     )
     _, _, reduce_artifact = _compile(reduce_program, _target("avx2"), tmp_path / "reduce")
-    values = [float(index) / 7.0 for index in range(30)]
+    pattern = [3.265625, 4.4375, 0.1630859375]
+    values = [
+        pattern[axis]
+        for _row in range(2)
+        for axis in range(3)
+        for _column in range(5)
+    ]
     avx_reduce = HostTensor.zeros("bf16", (2, 5))
     _run(reduce_artifact, [HostTensor("bf16", (2, 3, 5), values)], avx_reduce)
     scalar_compiler = CpuScalarCompiler()
@@ -458,9 +522,17 @@ def test_bf16_reduction_and_matmul_match_scalar(tmp_path):
     expected_reduce = []
     for row in range(2):
         for column in range(5):
-            total = sum(rounded_values[row * 15 + axis * 5 + column] for axis in range(3))
+            total = _float32(0.0)
+            for axis in range(3):
+                total = _float32_add(total, rounded_values[row * 15 + axis * 5 + column])
             expected_reduce.append(_bf16_value(_bf16_bits(total)))
     assert avx_reduce.flat() == tuple(expected_reduce)
+    fp_total = _float32(0.0)
+    rounded_step = _bf16_value(0)
+    for value in pattern:
+        fp_total = _float32_add(fp_total, value)
+        rounded_step = _bf16_value(_bf16_bits(_float32_add(rounded_step, value)))
+    assert _bf16_bits(fp_total) != _bf16_bits(rounded_step)
 
     left_type = CoreType.tensor("bf16", (2, 3))
     right_type = CoreType.tensor("bf16", (3, 9))
@@ -474,7 +546,7 @@ def test_bf16_reduction_and_matmul_match_scalar(tmp_path):
         (output,),
         (Operation("matmul", (left, right), (output,)),),
     )
-    _, _, matmul_artifact = _compile(matmul_program, _target("avx2", "fma"), tmp_path / "matmul")
+    _, _, matmul_artifact = _compile(matmul_program, _target("avx2"), tmp_path / "matmul")
     avx_matmul = HostTensor.zeros("bf16", (2, 9))
     left_values = [float(index + 1) / 3.0 for index in range(6)]
     right_values = [float(index + 1) / 5.0 for index in range(27)]
@@ -503,12 +575,21 @@ def test_bf16_reduction_and_matmul_match_scalar(tmp_path):
     expected_matmul = []
     for row in range(2):
         for column in range(9):
-            total = sum(
-                rounded_left[row * 3 + inner] * rounded_right[inner * 9 + column]
-                for inner in range(3)
-            )
+            total = _float32(0.0)
+            for inner in range(3):
+                product = _float32_mul(
+                    rounded_left[row * 3 + inner], rounded_right[inner * 9 + column]
+                )
+                total = _float32_add(total, product)
             expected_matmul.append(_bf16_value(_bf16_bits(total)))
     assert avx_matmul.flat() == tuple(expected_matmul)
+    strict_total = _float32(0.0)
+    rounded_step = _bf16_value(0)
+    for inner in range(3):
+        product = _float32_mul(rounded_left[inner], rounded_right[inner * 9 + 4])
+        strict_total = _float32_add(strict_total, product)
+        rounded_step = _bf16_value(_bf16_bits(_float32_add(rounded_step, product)))
+    assert _bf16_bits(strict_total) != _bf16_bits(rounded_step)
 
 
 def test_identity_constant_and_scalar_broadcast_use_native_elementwise(tmp_path):
@@ -624,7 +705,7 @@ def test_rectangular_matmul_uses_native_path(tmp_path):
     right = ValueRef("right", right_type)
     output = ValueRef("output", output_type)
     program = _program("matmul", (left, right), (output,), (Operation("matmul", (left, right), (output,)),))
-    _, _, artifact = _compile(program, _target("avx2", "fma"), tmp_path)
+    _, _, artifact = _compile(program, _target("avx2"), tmp_path)
     destination = HostTensor.zeros("float32", (2, 9))
     _run(
         artifact,
@@ -648,6 +729,9 @@ def test_rectangular_float_matmul_has_tolerant_scalar_differential(tmp_path):
     right = ValueRef("right", right_type)
     output = ValueRef("output", output_type)
     program = _program("matmul_float", (left, right), (output,), (Operation("matmul", (left, right), (output,)),))
+    runtime = _runtime()
+    if "fma" not in runtime.capability.features:
+        pytest.skip("host lacks FMA; FMA artifact execution is unavailable")
     _, _, artifact = _compile(program, _target("avx2", "fma"), tmp_path / "avx2")
     left_values = [0.1 + index * 0.017 for index in range(15)]
     right_values = [0.2 - index * 0.013 for index in range(35)]
@@ -656,6 +740,7 @@ def test_rectangular_float_matmul_has_tolerant_scalar_differential(tmp_path):
         artifact,
         [HostTensor("float32", (3, 5), left_values), HostTensor("float32", (5, 7), right_values)],
         avx_output,
+        runtime,
     )
     scalar_compiler = CpuScalarCompiler()
     scalar_artifact = scalar_compiler.compile(
@@ -682,8 +767,9 @@ def test_missing_feature_is_structured_and_does_not_load_artifact(tmp_path):
     status = runtime.availability()
     assert not status.available
     assert status.reason_code == "missing_cpu_features"
-    with pytest.raises(Exception):
+    with pytest.raises(CpuAvx2UnavailableError) as error:
         runtime.load(artifact)
+    assert error.value.status.reason_code == "missing_cpu_features"
 
 
 def test_feature_contract_rejects_fma_when_fake_host_lacks_it(tmp_path, monkeypatch):
@@ -749,7 +835,60 @@ def test_cache_key_isolated_by_fma_and_target(tmp_path):
     assert Path(shared_no_fma.path).read_bytes() == before
 
 
-def test_compiler_accepts_only_clang_and_runtime_rejects_wrong_format(tmp_path):
+def test_same_variant_concurrent_compile_uses_unique_temporary_paths(tmp_path):
+    compiler = Avx2Compiler()
+    target_ir = compiler.lower(_add_program(), _target("avx2"))
+    options = CompileOptions(
+        artifact_format="pypto-x.cpu.avx2",
+        extra={"build_dir": str(tmp_path / "shared-build"), "artifact_dir": str(tmp_path / "artifacts")},
+    )
+
+    def compile_once(_index):
+        return compiler.compile(target_ir, options)
+
+    with ThreadPoolExecutor(max_workers=2) as executor:
+        artifacts = list(executor.map(compile_once, (0, 1)))
+    assert artifacts[0].path == artifacts[1].path
+    assert Path(artifacts[0].path).is_file()
+    temporary = tmp_path / "shared-build" / "tmp"
+    assert not list(temporary.iterdir())
+
+
+def test_compiler_rejects_wrong_format_and_abi_before_native_compile(tmp_path, monkeypatch):
+    compiler = Avx2Compiler()
+    target_ir = compiler.lower(_add_program(), _target("avx2"))
+    valid = compiler.compile(
+        target_ir,
+        CompileOptions(artifact_format="pypto-x.cpu.avx2", extra={"build_dir": str(tmp_path / "valid")}),
+    )
+
+    def fail_native(*args, **kwargs):
+        raise AssertionError("native compiler must not run for invalid options")
+
+    monkeypatch.setattr(compiler_module, "_compile_native", fail_native)
+    with pytest.raises(Avx2CompileError, match="format"):
+        compiler.compile(
+            target_ir,
+            CompileOptions(
+                artifact_format="python",
+                extra={"build_dir": str(tmp_path / "wrong-format")},
+            ),
+        )
+    with pytest.raises(Avx2CompileError, match="ABI"):
+        compiler.compile(
+            target_ir,
+            CompileOptions(
+                artifact_format="pypto-x.cpu.avx2",
+                abi_version="2",
+                extra={"build_dir": str(tmp_path / "wrong-abi")},
+            ),
+        )
+    object.__setattr__(valid, "format", "python")
+    with pytest.raises(CpuAvx2ArtifactError, match="format"):
+        Avx2Runtime().load(valid)
+
+
+def test_compiler_accepts_only_clang(tmp_path):
     compiler = Avx2Compiler()
     target_ir = compiler.lower(_add_program(), _target("avx2"))
     with pytest.raises(Avx2CompileError, match="Clang"):
@@ -760,12 +899,6 @@ def test_compiler_accepts_only_clang_and_runtime_rejects_wrong_format(tmp_path):
                 extra={"build_dir": str(tmp_path / "gcc"), "clang": "gcc"},
             ),
         )
-    wrong_format = compiler.compile(
-        target_ir,
-        CompileOptions(artifact_format="python", extra={"build_dir": str(tmp_path / "wrong-format")}),
-    )
-    with pytest.raises(CpuAvx2ArtifactError, match="format"):
-        Avx2Runtime().load(wrong_format)
 
 
 def test_native_variant_marker_binds_metadata_to_binary(tmp_path):
@@ -787,10 +920,19 @@ def test_objdump_has_ymm_and_no_zmm(tmp_path):
         result = subprocess.run(
             [objdump, "-d", path], stdout=subprocess.PIPE, universal_newlines=True, check=True
         )
-        return result.stdout.lower()
-
-    fma_text = disassemble(fma_artifact.path)
-    no_fma_text = disassemble(no_fma_artifact.path)
+        text = result.stdout.lower()
+        first_bytes = []
+        for line in text.splitlines():
+            match = re.match(r"^\s*[0-9a-f]+:\s+((?:[0-9a-f]{2}\s+)+)", line)
+            if match:
+                first_bytes.append(int(match.group(1).split()[0], 16))
+        assert first_bytes
+        return text, first_bytes
+
+    fma_text, fma_first_bytes = disassemble(fma_artifact.path)
+    no_fma_text, no_fma_first_bytes = disassemble(no_fma_artifact.path)
+    assert 0x62 not in fma_first_bytes
+    assert 0x62 not in no_fma_first_bytes
     for text in (fma_text, no_fma_text):
         assert "ymm" in text
         assert "zmm" not in text
