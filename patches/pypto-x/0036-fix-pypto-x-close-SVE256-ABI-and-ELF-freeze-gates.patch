From a00a62ef01c421f9ac9b2db52f021db149bf5ff3 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Tue, 8 Sep 2026 13:39:48 +0800
Subject: [PATCH 036/250] fix(pypto-x): close SVE256 ABI and ELF freeze gates

---
 python/pypto/backends/cpu/aarch64/sve256.py       |  46 ++++++++--
 python/pypto/compiler/targets/cpu_sve256.py       |   7 +-
 python/pypto/lowering/cpu/aarch64/sve/lowering.py |  19 ++++-
 python/tests/ut/pypto_x/test_cpu_sve256.py        | 192 +++++++++++++++++++++++++++++++++++++++++-
 4 files changed, 250 insertions(+), 14 deletions(-)

diff --git a/python/pypto/backends/cpu/aarch64/sve256.py b/python/pypto/backends/cpu/aarch64/sve256.py
index 023ec0f38..2ac522289 100644
--- a/python/pypto/backends/cpu/aarch64/sve256.py
+++ b/python/pypto/backends/cpu/aarch64/sve256.py
@@ -37,6 +37,7 @@ from pypto.lowering.cpu.aarch64.sve import (
     Sve256Program,
     lower_sve256,
 )
+from pypto.lowering.cpu.aarch64.sve.lowering import _is_little_linux_aarch64
 from pypto.lowering.cpu.aarch64.sve import capabilities as _capabilities
 from pypto.lowering.cpu.vector import (
     CpuFeatureContract,
@@ -186,6 +187,23 @@ def _read_native_bytes(path: str) -> bytes:
         os.close(descriptor)
 
 
+def _validate_elf_header(native_bytes: bytes) -> None:
+    """Reject non-AArch64, big-endian, or non-executable ELF before launch."""
+
+    if len(native_bytes) < 64 or native_bytes[:4] != b"\x7fELF":
+        raise CpuSve256ArtifactError("SVE256 artifact is not an ELF file")
+    if native_bytes[4] != 2:
+        raise CpuSve256ArtifactError("SVE256 artifact is not ELF64")
+    if native_bytes[5] != 1:
+        raise CpuSve256ArtifactError("SVE256 artifact is not little-endian")
+    elf_type = struct.unpack_from("<H", native_bytes, 16)[0]
+    machine = struct.unpack_from("<H", native_bytes, 18)[0]
+    if machine != 183:
+        raise CpuSve256ArtifactError("SVE256 artifact machine is not AArch64")
+    if elf_type not in (2, 3):
+        raise CpuSve256ArtifactError("SVE256 artifact is not an executable or PIE ELF")
+
+
 def _string_sequence(value: Any, context: str) -> Tuple[str, ...]:
     if not isinstance(value, (list, tuple)):
         raise CpuSve256ArtifactError("{} must be a sequence of strings".format(context))
@@ -238,8 +256,10 @@ def _validate_primary_artifact(artifact: Artifact) -> Mapping[str, Any]:
         raise CpuSve256ArtifactError("SVE256 artifact must reference an AArch64 ELF executable")
     if artifact.format != DEFAULT_ARTIFACT_FORMAT or artifact.abi_version != ABI_VERSION:
         raise CpuSve256ArtifactError("SVE256 artifact format or ABI is unsupported")
-    if artifact.target.kind != "cpu" or not artifact.target.triple.startswith(("aarch64", "arm64")):
-        raise CpuSve256ArtifactError("SVE256 artifact target must be AArch64 CPU")
+    if artifact.target.kind != "cpu" or not _is_little_linux_aarch64(artifact.target.triple):
+        raise CpuSve256ArtifactError(
+            "SVE256 artifact target must be little-endian Linux AArch64 CPU"
+        )
     metadata = mapping_copy(artifact.metadata)
     if metadata.get("backend") != SVE256_BACKEND:
         raise CpuSve256ArtifactError("SVE256 artifact backend metadata is invalid")
@@ -263,6 +283,7 @@ def _validate_primary_artifact(artifact: Artifact) -> Mapping[str, Any]:
 def _decode_artifact(artifact: Artifact, available: CpuFeatureSet) -> LoadedSve256Artifact:
     metadata = _validate_primary_artifact(artifact)
     native_bytes = _read_native_bytes(artifact.path)
+    _validate_elf_header(native_bytes)
     actual_digest = hashlib.sha256(native_bytes).hexdigest()
     if actual_digest != artifact.content_digest or metadata.get("native_digest") != actual_digest:
         raise CpuSve256ArtifactError("SVE256 ELF content digest mismatch")
@@ -359,9 +380,10 @@ def _storage_values(descriptor: TensorDesc, dtype: str, context: str) -> List[An
             raise CpuSve256ExecutionError("{} tensor address is null".format(context))
         ctype = _CTYPE[dtype]
         try:
-            values = list((ctype * count).from_address(storage))
+            raw_values = list((ctype * count).from_address(storage))
         except (TypeError, ValueError, OSError) as exc:
             raise CpuSve256ExecutionError("{} tensor address is invalid".format(context)) from exc
+        values = [_bf16_value(value) for value in raw_values] if dtype == "bf16" else raw_values
     elif isinstance(storage, ctypes.Array):
         ctype = getattr(storage, "_type_", None)
         expected_ctype = _CTYPE[dtype]
@@ -443,12 +465,14 @@ def _write_storage(descriptor: TensorDesc, dtype: str, values: Sequence[Any], co
         storage.data[:] = values
         return
     if isinstance(storage, FlatBuffer):
+        if len(storage.data) != descriptor.element_count:
+            raise CpuSve256ExecutionError("{} FlatBuffer length does not match descriptor".format(context))
         storage.data[:] = values
         return
     if isinstance(storage, ctypes.Array):
         ctype = getattr(storage, "_type_", None)
         expected_ctype = _CTYPE[dtype]
-        if ctype is not expected_ctype or len(storage) < descriptor.element_count:
+        if ctype is not expected_ctype or len(storage) != descriptor.element_count:
             raise CpuSve256ExecutionError("{} ctypes output buffer is incompatible".format(context))
         for index, value in enumerate(values):
             storage[index] = _bf16_bits(value) if dtype == "bf16" else float(value)
@@ -499,7 +523,15 @@ def _write_storage(descriptor: TensorDesc, dtype: str, values: Sequence[Any], co
             return
         except (AttributeError, TypeError, ValueError) as exc:
             raise CpuSve256ExecutionError("{} output array is not writable".format(context)) from exc
+    if isinstance(storage, list):
+        if len(storage) != descriptor.element_count:
+            raise CpuSve256ExecutionError("{} list length does not match descriptor".format(context))
+        for index, value in enumerate(values):
+            storage[index] = value
+        return
     if hasattr(storage, "data") and isinstance(storage.data, list):
+        if len(storage.data) != descriptor.element_count:
+            raise CpuSve256ExecutionError("{} handle data length does not match descriptor".format(context))
         storage.data[:] = values
         return
     try:
@@ -781,8 +813,10 @@ class Sve256Runtime:
         loaded = self._decode_for_execution(request.artifact, capability)
         self._selected_artifact = request.artifact
         target = loaded.target_ir.target
-        if target.kind != "cpu" or not target.triple.startswith(("aarch64", "arm64")):
-            raise CpuSve256ExecutionError("SVE256 artifact target must be AArch64 CPU")
+        if target.kind != "cpu" or not _is_little_linux_aarch64(target.triple):
+            raise CpuSve256ExecutionError(
+                "SVE256 artifact target must be little-endian Linux AArch64 CPU"
+            )
         for descriptor in request.inputs + request.outputs:
             if descriptor.device != "cpu":
                 raise CpuSve256ExecutionError("SVE256 requires cpu tensor descriptors")
diff --git a/python/pypto/compiler/targets/cpu_sve256.py b/python/pypto/compiler/targets/cpu_sve256.py
index 610350790..5f4620d63 100644
--- a/python/pypto/compiler/targets/cpu_sve256.py
+++ b/python/pypto/compiler/targets/cpu_sve256.py
@@ -659,8 +659,11 @@ def _compiler_info(compiler_path: str) -> tuple:
         universal_newlines=True, check=False,
     )
     triple = triple_result.stdout.strip() or "unknown"
-    if not triple.lower().startswith(("aarch64", "arm64")):
-        raise Sve256CompileError("compiler target triple {!r} is not AArch64".format(triple))
+    parts = triple.lower().split("-")
+    if len(parts) < 2 or parts[0] != "aarch64" or parts[1] != "linux":
+        raise Sve256CompileError(
+            "compiler target triple {!r} is not little-endian Linux AArch64".format(triple)
+        )
     return version, triple
 
 
diff --git a/python/pypto/lowering/cpu/aarch64/sve/lowering.py b/python/pypto/lowering/cpu/aarch64/sve/lowering.py
index 57b524045..77549e59d 100644
--- a/python/pypto/lowering/cpu/aarch64/sve/lowering.py
+++ b/python/pypto/lowering/cpu/aarch64/sve/lowering.py
@@ -83,6 +83,13 @@ def _target_vl_bytes(target: TargetSpec) -> Optional[int]:
     return None
 
 
+def _is_little_linux_aarch64(triple: str) -> bool:
+    if not isinstance(triple, str):
+        return False
+    parts = triple.strip().lower().split("-")
+    return len(parts) >= 2 and parts[0] == "aarch64" and parts[1] == "linux"
+
+
 def _normalised_target(target: TargetSpec) -> TargetSpec:
     # ``sve2`` is intentionally retained only by the capability probe.  It is
     # not an emitted feature of this first SVE lowering, so it must not flow
@@ -161,8 +168,10 @@ class Sve256Program:
         target = self.vector_program.target
         if target.kind != "cpu":
             raise Sve256ValidationError("SVE256 target requires kind='cpu'")
-        if not target.triple.startswith(("aarch64", "arm64")):
-            raise Sve256ValidationError("SVE256 target requires an AArch64 triple")
+        if not _is_little_linux_aarch64(target.triple):
+            raise Sve256ValidationError(
+                "SVE256 target requires little-endian Linux AArch64 triple"
+            )
         if self.vector_program.lane_count != SVE256_LANE_COUNT:
             raise Sve256ValidationError("SVE256 requires 8 float32 lanes")
         if self.vector_program.tail_policy != SVE256_TAIL_POLICY:
@@ -255,8 +264,10 @@ def lower_sve256(
         raise Sve256ValidationError("SVE256 lowering requires TargetSpec")
     if target.kind != "cpu":
         raise Sve256ValidationError("SVE256 target requires kind='cpu'")
-    if not target.triple.startswith(("aarch64", "arm64")):
-        raise Sve256ValidationError("SVE256 target requires an AArch64 triple")
+    if not _is_little_linux_aarch64(target.triple):
+        raise Sve256ValidationError(
+            "SVE256 target requires little-endian Linux AArch64 triple"
+        )
     declared = set(_features(target.features))
     unknown = sorted(declared.difference(_TARGET_FEATURES))
     if unknown:
diff --git a/python/tests/ut/pypto_x/test_cpu_sve256.py b/python/tests/ut/pypto_x/test_cpu_sve256.py
index 328a4f21f..fc1042b59 100644
--- a/python/tests/ut/pypto_x/test_cpu_sve256.py
+++ b/python/tests/ut/pypto_x/test_cpu_sve256.py
@@ -3,6 +3,8 @@
 from __future__ import annotations
 
 import ctypes
+from concurrent.futures import ThreadPoolExecutor
+import hashlib
 import os
 import platform
 import re
@@ -15,7 +17,7 @@ import sys
 import pytest
 
 from pypto.abi import LaunchRequest, TensorDesc
-from pypto.backends.cpu import CpuScalarRuntime, HostTensor
+from pypto.backends.cpu import CpuScalarRuntime, FlatBuffer, HostTensor
 from pypto.backends.cpu.aarch64 import (
     CpuSve256ArtifactError,
     CpuSve256ExecutionError,
@@ -25,7 +27,7 @@ from pypto.backends.cpu.aarch64 import (
 )
 from pypto.compiler import Artifact, CompileOptions
 from pypto.compiler.targets.cpu import CpuScalarCompiler
-from pypto.compiler.targets.cpu_sve256 import Sve256Compiler
+from pypto.compiler.targets.cpu_sve256 import Sve256CompileError, Sve256Compiler
 from pypto.core_ir import CoreFunction, CoreProgram, CoreType, Operation, Region, ValueRef
 from pypto.lowering.cpu.aarch64.sve import (
     SVE256_LANE_COUNT,
@@ -175,6 +177,65 @@ def test_lowering_uses_eight_lanes_masked_tail_and_omits_sve2():
         lower_sve256(_elementwise(), _target("sve", "neon"))
 
 
+def test_sve_target_is_fixed_little_endian_linux_protocol():
+    for triple in ("aarch64_be-linux-gnu", "aarch64-unknown-linux-gnu", "aarch64-none-elf"):
+        with pytest.raises(Sve256ValidationError, match="little-endian Linux"):
+            lower_sve256(_elementwise(), TargetSpec("cpu", triple, features=("sve",)))
+
+
+def test_sve_compiler_rejects_format_and_abi_before_native_compile(tmp_path, monkeypatch):
+    compiler = Sve256Compiler()
+    target_ir = compiler.lower(_elementwise(), _target("sve"))
+    import pypto.compiler.targets.cpu_sve256 as compiler_module
+
+    monkeypatch.setattr(
+        compiler_module,
+        "_compile_native",
+        lambda *args, **kwargs: (_ for _ in ()).throw(AssertionError("native compile")),
+    )
+    with pytest.raises(Sve256CompileError, match="artifact format"):
+        compiler.compile(
+            target_ir,
+            CompileOptions(artifact_format="python", extra={"build_dir": str(tmp_path)}),
+        )
+    with pytest.raises(Sve256CompileError, match="ABI"):
+        compiler.compile(
+            target_ir,
+            CompileOptions(
+                artifact_format="pypto-x.cpu.sve256",
+                abi_version="future",
+                extra={"build_dir": str(tmp_path)},
+            ),
+        )
+
+
+def test_sve_cache_key_is_build_path_independent_and_publication_is_race_safe(tmp_path):
+    program = _elementwise(shape=(9,))
+    target = _target("sve")
+
+    def compile_once(build_dir):
+        compiler = Sve256Compiler()
+        target_ir = compiler.lower(program, target)
+        return compiler.compile(
+            target_ir,
+            CompileOptions(
+                artifact_format="pypto-x.cpu.sve256",
+                extra={
+                    "build_dir": str(build_dir),
+                    "artifact_dir": str(tmp_path / "artifacts"),
+                },
+            ),
+        )
+
+    first = compile_once(tmp_path / "build-a")
+    second = compile_once(tmp_path / "build-b")
+    assert first.cache_key == second.cache_key
+    with ThreadPoolExecutor(max_workers=2) as executor:
+        results = list(executor.map(compile_once, (tmp_path / "build-c", tmp_path / "build-d")))
+    assert all(result.cache_key == first.cache_key for result in results)
+    assert all(Path(result.path).stat().st_nlink == 1 for result in results)
+
+
 @pytest.mark.parametrize("operation", ("add", "sub", "mul", "div", "neg"))
 @pytest.mark.parametrize("length", (0, 1, 7, 8, 9, 19))
 def test_sve_elementwise_predicated_tail_matches_scalar(operation, length, tmp_path):
@@ -280,6 +341,26 @@ def test_sve_rectangular_matmul_odd_tail_matches_scalar(tmp_path):
     assert compare_outputs(expected, actual, atol=2e-5, rtol=2e-5).passed
 
 
+@pytest.mark.parametrize(
+    "left_shape,right_shape,output_shape",
+    (((2, 0), (0, 3), (2, 3)), ((0, 2), (2, 3), (0, 3)), ((2, 3), (3, 0), (2, 0))),
+)
+def test_sve_bf16_zero_dimension_matmul_matches_scalar(
+    left_shape, right_shape, output_shape, tmp_path
+):
+    program = _matmul("bf16", left_shape, right_shape)
+    _, _, artifact = _compile(program, tmp_path / "sve")
+    left = HostTensor.zeros("bf16", left_shape)
+    right = HostTensor.zeros("bf16", right_shape)
+    actual = HostTensor.zeros("bf16", output_shape)
+    expected = HostTensor.zeros("bf16", output_shape)
+    _run(artifact, [left, right], actual)
+    _scalar_run(program, [left, right], expected)
+    assert actual.flat() == expected.flat()
+    if output_shape and output_shape[0] and output_shape[1]:
+        assert actual.flat() == (0.0,) * (output_shape[0] * output_shape[1])
+
+
 def test_sve_bf16_software_conversion_uses_fp32_accumulation_and_rne(tmp_path):
     program = _matmul("bf16", (2, 3), (3, 19))
     _, _, artifact = _compile(program, tmp_path)
@@ -443,6 +524,69 @@ def test_sve_numpy_ctypes_and_bf16_bits_buffer_abi(tmp_path):
     assert set(struct.unpack("<{}H".format(18), v2_output.tobytes())) == {0x4000}
 
 
+def test_sve_bf16_raw_address_uses_bits_and_preserves_canaries(tmp_path):
+    program = _elementwise(dtype="bf16", shape=(9,))
+    _, _, artifact = _compile(program, tmp_path)
+    raw_left = (ctypes.c_ubyte * (9 * 2 + 2))()
+    raw_right = (ctypes.c_ubyte * (9 * 2 + 2))()
+    raw_output = (ctypes.c_ubyte * (9 * 2 + 2))()
+    left_address = ctypes.addressof(raw_left) + 1
+    right_address = ctypes.addressof(raw_right) + 1
+    output_address = ctypes.addressof(raw_output) + 1
+    left = ctypes.cast(left_address, ctypes.POINTER(ctypes.c_uint16))
+    right = ctypes.cast(right_address, ctypes.POINTER(ctypes.c_uint16))
+    output = ctypes.cast(output_address, ctypes.POINTER(ctypes.c_uint16))
+    raw_output[0] = 0xA5
+    raw_output[-1] = 0x5A
+    for index in range(9):
+        left[index] = 0x3F80
+        right[index] = 0x3F80
+        output[index] = 0
+    Sve256Runtime(execution_mode="qemu").launch(
+        LaunchRequest(
+            artifact,
+            inputs=[
+                TensorDesc("bf16", (9,), address=left_address, readonly=True),
+                TensorDesc("bf16", (9,), address=right_address, readonly=True),
+            ],
+            outputs=[TensorDesc("bf16", (9,), address=output_address)],
+        )
+    )
+    assert [output[index] for index in range(9)] == [0x4000] * 9
+    assert raw_output[0] == 0xA5 and raw_output[-1] == 0x5A
+
+
+def test_sve_output_buffers_require_exact_lengths(tmp_path):
+    program = _elementwise(shape=(9,))
+    _, _, artifact = _compile(program, tmp_path)
+    left = HostTensor("float32", (9,), list(range(9)))
+    right = HostTensor("float32", (9,), [1.0] * 9)
+    runtime = Sve256Runtime(execution_mode="qemu")
+    for output in (
+        [0.0] * 8,
+        [0.0] * 10,
+        type("Handle", (), {"data": [0.0] * 8})(),
+        type("Handle", (), {"data": [0.0] * 10})(),
+    ):
+        with pytest.raises(CpuSve256ExecutionError, match="length|storage"):
+            runtime.launch(
+                LaunchRequest(
+                    artifact,
+                    inputs=[left.descriptor(readonly=True), right.descriptor(readonly=True)],
+                    outputs=[TensorDesc("float32", (9,), handle=output)],
+                )
+            )
+    for output in (FlatBuffer.zeros(8), FlatBuffer.zeros(10)):
+        with pytest.raises(CpuSve256ExecutionError, match="length"):
+            runtime.launch(
+                LaunchRequest(
+                    artifact,
+                    inputs=[left.descriptor(readonly=True), right.descriptor(readonly=True)],
+                    outputs=[TensorDesc("float32", (9,), handle=output)],
+                )
+            )
+
+
 def test_sve_default_runtime_is_native_only_and_qemu_is_opt_in():
     runtime = Sve256Runtime()
     assert runtime.execution_mode == "native"
@@ -469,6 +613,22 @@ def test_sve_runner_rejects_wrong_qemu_vl_before_execution(tmp_path):
             assert result.returncode != 0
 
 
+def test_sve_missing_qemu_and_nonzero_runner_are_structured(tmp_path, monkeypatch):
+    _, _, artifact = _compile(_elementwise(shape=(1,)), tmp_path)
+    capability = Sve256Capability.injected_capability(("sve", "vl256"), emulated=True)
+    runtime = Sve256Runtime(
+        capability=capability, execution_mode="qemu", qemu="/missing/qemu-aarch64"
+    )
+    with pytest.raises(CpuSve256ExecutionError, match="qemu-aarch64"):
+        runtime._run_runner(artifact, "identity", "float32", [[1.0]], 1)
+
+    qemu_runtime = Sve256Runtime(
+        capability=capability, execution_mode="qemu", qemu_cpu="max,sve128=on"
+    )
+    with pytest.raises(CpuSve256ExecutionError, match="structured response|runner"):
+        qemu_runtime._run_runner(artifact, "identity", "float32", [[1.0]], 1)
+
+
 def test_sve_elf_guarded_buffer_self_test_runs_under_qemu(tmp_path):
     _, _, artifact = _compile(_elementwise(shape=(19,)), tmp_path)
     assert Sve256Runtime(execution_mode="qemu").guarded_buffer_self_test(artifact)
@@ -517,6 +677,33 @@ def test_sve_artifact_digest_metadata_and_hardlink_protection(tmp_path):
     with pytest.raises(CpuSve256ArtifactError):
         Sve256Runtime(execution_mode="qemu").load(linked)
 
+    fake_path = tmp_path / "fake.elf"
+    fake_bytes = b"\x7fELF" + b"\x02\x02" + b"\x01" + b"\x00" * 101
+    fake_path.write_bytes(fake_bytes)
+    fake_metadata = dict(artifact.metadata)
+    fake_metadata["native_digest"] = hashlib.sha256(fake_bytes).hexdigest()
+    fake = Artifact(
+        format=artifact.format,
+        entrypoint=artifact.entrypoint,
+        target=artifact.target,
+        abi_version=artifact.abi_version,
+        path=str(fake_path),
+        metadata=fake_metadata,
+    )
+    with pytest.raises(CpuSve256ArtifactError, match="ELF64|little-endian"):
+        Sve256Runtime(execution_mode="qemu").load(fake)
+
+    wrong_target = Artifact(
+        format=artifact.format,
+        entrypoint=artifact.entrypoint,
+        target=TargetSpec("cpu", "aarch64_be-linux-gnu", features=("sve",)),
+        abi_version=artifact.abi_version,
+        path=artifact.path,
+        metadata=artifact.metadata,
+    )
+    with pytest.raises(CpuSve256ArtifactError, match="little-endian Linux"):
+        Sve256Runtime(execution_mode="qemu").load(wrong_target)
+
     _, _, marker_artifact = _compile(_elementwise(), tmp_path / "marker")
     marker_metadata = dict(marker_artifact.metadata)
     marker_metadata["native_source_digest"] = "0" * 64
@@ -621,6 +808,7 @@ def test_sve_objdump_proves_predicated_sve_and_no_neon(tmp_path):
     assert "whilelo" in function_text
     assert re.search(r"\bz[0-9]+\.[sdh]\b", function_text)
     assert re.search(r"\bp[0-9]+\.[bhsd]\b", function_text)
+    assert not re.search(r"\b[vq][0-9]+(?:\.[bhsd])?\b", function_text)
     assert "neon" not in text
     readelf = shutil.which("aarch64-linux-gnu-readelf")
     if readelf is not None:
