From 1f0882c070047b67a8d2e9124996f0eb4de36a09 Mon Sep 17 00:00:00 2001
From: Chiro <chiro2001@163.com>
Date: Fri, 11 Sep 2026 20:51:06 +0800
Subject: [PATCH 137/144] docs(portable): account for the optional 321st
 dual-packing parameter

---
 python/pypto/portable/qwen35_weight_map.py | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/python/pypto/portable/qwen35_weight_map.py b/python/pypto/portable/qwen35_weight_map.py
index 0596e695a..1c4c0fa6f 100644
--- a/python/pypto/portable/qwen35_weight_map.py
+++ b/python/pypto/portable/qwen35_weight_map.py
@@ -6,7 +6,7 @@
 ``layers.linear_attention.*`` / ``layers.*`` 三类模式描述了 22 个存储类；
 本模块只把这三类模式展开成官方 HuggingFace 命名，再与 graph metadata 的
 ``layers[].parameter_routes`` 和 :class:`~pypto.portable.runtime_binding.BindingSchema`
-的 320 个精确函数参数逐一对照。
+的 320 个精确函数参数逐一对照（W8A8 全开时含 1 个共享来源的 dual-packing 参数，共 321 条）。
 
 三类名字必须同时成立，任何一方不匹配都 fail closed：
 
@@ -240,7 +240,7 @@ class TiedEmbedding:
 
 @dataclass(frozen=True)
 class WeightMapping:
-    """320 个文本参数到官方 checkpoint 的完整映射与排除清单。"""
+    """320 个文本存储类（W8A8 全开 321 个逻辑认领）到官方 checkpoint 的完整映射与排除清单。"""
 
     graph_digest: str
     checkpoint_path: str
@@ -404,7 +404,7 @@ def build_weight_mapping(
     *,
     tie_word_embeddings: bool = True,
 ) -> WeightMapping:
-    """把 320 个 Core 参数映射到官方 checkpoint 张量。
+    """把 320 个 Core 参数（W8A8 全开 321 个）映射到官方 checkpoint 张量。
 
     任何未映射参数、缺失张量、未预期张量或 shape/元素数不一致都会抛出
     :class:`WeightMapMismatchError`。dtype 差异**不**抛异常，而是逐条记录在
