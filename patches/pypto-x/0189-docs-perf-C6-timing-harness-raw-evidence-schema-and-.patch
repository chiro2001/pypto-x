From 9ad9d4801c4c6b59d2094425b44ea0cbda12f0b1 Mon Sep 17 00:00:00 2001
From: PyPTO-X Agent <agent@pypto-x.local>
Date: Sat, 12 Sep 2026 03:07:55 +0800
Subject: [PATCH 189/257] docs(perf): C6 timing harness raw-evidence schema and
 run procedure

---
 scripts/perf/README-c6-timing.md | 85 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 85 insertions(+)

diff --git a/scripts/perf/README-c6-timing.md b/scripts/perf/README-c6-timing.md
new file mode 100644
index 000000000..08bd5888c
--- /dev/null
+++ b/scripts/perf/README-c6-timing.md
@@ -0,0 +1,85 @@
+# C6 W8A8 CUDA 计时证据工具链（measurement only）
+
+任务 `c6-timing-evidence`。本目录只做测量，不改 runtime / compiler / 契约源码。
+
+## 文件
+
+| 文件 | 作用 |
+|---|---|
+| `c6_timing_harness.py` | 在 GamePC（RTX 5080 / WSL2）上对 C6 的 4 个 W8A8 opcode × W8J 12 形状做 cuEvent 计时；含窗口内 rate 标定、warmup、R 轮、ERR-0004 外点标记、clock warmup、窗口污染采样、公开入口 dispatch 上界、vendor 两趟对照与 bf16 平台基线、交错配对采样 |
+| `c6_timing_analyze.py` | 离线分析：ERR-0004 / 协议方差门 / B3a rate 门，输出 `analysis.json` 与 `tables.zh-CN.md`（全部 `UNGATED`） |
+| `c6_vendor_shim.c` | 经典 `cublasGemmEx` 的 C 边界 shim。直接 ctypes 按文档签名调用在 GamePC CUDA 13.3 上返回 status 7（"parameter number 12"），同一份参数的 C 调用返回 0，故测量走 shim |
+| `c6_timing_selftest.py` | 本地（无 GPU）mock 自检：launch 映射、缓冲尺寸、正确性 check、verdict 逻辑 |
+| `c6_timing_gamepc.sh` | GamePC 运行脚本：`sync`/`shim`/`smoke`/`run`/`fetch`/`clean`；GPU-only，不申请 `gamepc` 锁，运行结束删除远端 `~/c6-timing` |
+
+## 运行
+
+```bash
+# 本地无 GPU 自检
+PYPTO_X_PORTABLE_ONLY=1 python3 scripts/perf/c6_timing_selftest.py
+
+# 本地只编译 48 个 artifact
+PYPTO_X_PORTABLE_ONLY=1 PYTHONPATH=python python3 scripts/perf/c6_timing_harness.py \
+    --build-only --output /tmp/c6-build.json
+
+# GamePC 全量（sync + shim + build-only smoke + run + fetch + clean）
+C6_RAW_NAME=c6-timing-raw-r3.json \
+C6_HARNESS_ARGS="--rounds 51 --warmup 5 --dispatch-rounds 21 --preflight-max-utilization-pct 55" \
+bash scripts/perf/c6_timing_gamepc.sh run
+
+# 离线分析
+python3 scripts/perf/c6_timing_analyze.py --raw <raw.json> --output <analysis-dir>
+```
+
+## raw JSON 落盘格式（`artifact_kind = pypto-x.c6-w8a8-timing-raw`）
+
+顶层字段：
+
+```text
+artifact_kind, schema_version, task, status, started_at_utc, finished_at_utc,
+git, git_branch, worktree, argv, python, platform,
+protocol{name,gated,ungated_reason,warmup,rounds,outlier_rule,variance_limits_*,rate_calibration},
+shape_set{source,rows,m_values,opcode_shape_mapping}, config,
+device, w8a8_probe, gpu_fingerprint_before/during/after, gpu_preflight,
+clock_warmup{seconds_actual,launches,clocks_sm_mhz_samples,clocks_sm_mhz_median,
+             clocks_max_sm_mhz,clock_ratio_median,clock_verdict,gpu_samples},
+rate_calibration_start/end{gap_a,gap_b{rates,median_rate,rate_offset_pct,spread_pct,spans}},
+wall_agreement{batches[{repeats,event_seconds,host_wall_seconds,event_per_launch_seconds,
+                        host_per_launch_seconds,raw_wall_agreement_pct}],linearity_pct},
+rate_calibration_window{window_rate_median,rate_offset_pct,rate_spread_pct,linearity_pct,
+                        corrected_wall_agreement_pct,corrected_wall_agreement_pct_max,...},
+gpu_samples_during_measurement, window_contamination{...shared_window},
+cases[]{
+  case_id, opcode, m, k, n, w8j_row_name,
+  correctness{method,status,...}, kernel_name, grid, block, launch_count,
+  warmup_kernel_seconds[5], warmup_entry_wall_seconds[5],
+  measured_kernel_seconds[R], measured_entry_wall_seconds[R],
+  batch{repeats,span_seconds,per_launch_seconds,host_wall_seconds,
+        host_wall_per_launch_seconds,raw_wall_agreement_pct},
+  gop_per_s, flop_accounting, seconds
+},
+dispatch_overhead[{opcode,shape,rounds,entry_wall_seconds[],kernel_seconds[],
+                   entry_wall_median_seconds,kernel_median_seconds,dispatch_median_seconds}],
+vendor_two_pass{mode,lib_path,version,status,shapes[]{
+  m,k,n,gemm_status,bf16_status,int32_mismatches_vs_c6,
+  two_pass_bf16_mismatches_vs_fused,
+  gemm_vendor_int8/gemm_ours_dp4a/epilogue_ours/two_pass_combined/
+  fused_path_combined/bf16_gemm{rounds,kernel_seconds[],median_seconds,...},
+  paired_vendor_vs_ours_gemm/paired_two_pass_vs_fused/paired_bf16_vs_ours_gemm{
+    paired_ratio_*{median_ratio,ratios[]}},
+  gemm_host_call_seconds{...}, bf16_host_call_seconds{...}}},
+load_seconds
+```
+
+`analysis.json`（`artifact_kind = pypto-x.c6-w8a8-timing-analysis`）在 raw 之上给出每 case 的
+`raw`/`corrected` 统计块、ERR-0004 外点与方差 verdict、rate gate、vendor 配对比值与 dispatch 一致性。
+
+## 口径提醒
+
+- 所有数字 `UNGATED`：WSL2、无 cpufreq/时钟锁定、共享非静默窗口（Graph2 纪律：GPU-only 不持锁）。
+- `kernel_seconds` 是 cuEvent 在 launch 两侧的跨度；模块 JIT 与 H2D/D2H 在 event 对之外。
+  对 <30 µs 的 kernel，该平台（host 编组 + launch + 共享窗口调度）已分辨不出纯 device 时间，
+  这类数字按"有效延迟"读，并把跨运行离散度一并报出。
+- corrected = raw × 窗口内标定的 `window_rate_median`；本会话速率偏移小但**窗口内会漂移**，
+  标定不确定度约 ±1.5%（见 brief §2）。
+- bf16 基线是平台 GEMM 基线，不是 W8A8 端到端基线；`cuBLASLt` 融合 int8 在 sm_120 不可用（Q8b）。
