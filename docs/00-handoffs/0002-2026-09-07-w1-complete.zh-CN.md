# PyPTO-X W1 完成交接快照

归档序号：`0002`

归档日期：2026-09-07（Asia/Shanghai）

状态：`W1_COMPLETE_W2_READY`

## 冻结点

```text
base        = 34475e0d83c6cdc7deac2082b1b4fa81b3beb6ad
integration = 2ab2f2ca59bd86dee654b55a0e97b7d25af966e9
branch      = port/pypto-x-integration
```

详细 task→commit 映射见 [开发集成锁](../../configs/development_lock.yaml)。

## 已完成能力

- Tensor-first `CoreProgram` 数据模型、effect、SSA 验证和 schema v1 canonical JSON。
- 显式纯 Python Tensor export input 与 native bridge seam；没有伪造 native parser 已连通。
- `TargetSpec`、`CapabilitySet`、显式覆盖语义的 target registry。
- `CompilerBackend`、immutable `Artifact`、稳定 cache key 与文件篡改检测。
- `TensorDesc`、`LaunchRequest`、`RuntimeBackend` 和结构化 unavailable 原因。
- 无设备数值差分报告与默认只读、显式更新的 IR snapshot harness。
- setuptools 精确包含新增 portable 子包。

## 验证证据

- 三个 task 的唯一一次 host smoke：全部 `PASS`。
- integration 同一进程联合测试：`23 passed`。
- Core IR 与 snapshot harness 互操作：`PASS`。
- package discovery：`PASS`。
- integration smoke：`PASS`，同时 compileall `pypto` 与 `pypto_pro`。

## 未完成与门禁

- 实际 Tensor PIL/native 解析结果尚未结构化导出到 Core IR；W2 增加 `tensor-core-bridge`。
- CPU scalar/AVX/SVE/CUDA/HIP 尚未实现。
- Ascend stable lock 仍缺真实 CANN toolkit/NPU 验证。
- 源码 checkout 直接执行 `pypto.__init__` 仍依赖上游 native online-build；W1 纯 Python测试使用隔离 namespace，不能表述为已解决安装问题。

## W2

W2 从同一 integration 冻结点创建三个独立任务：

1. `tensor-core-bridge`：PIL/native 结果到 Core IR；未支持节点显式失败。
2. `cpu-scalar`：add/reduce/matmul 可执行 reference，并接入 differential harness。
3. `ascend-adapter`：依赖注入/lazy import 的 CCE seam；无 NPU 时只验证接口，不宣称真机回归。

契约见 [W2 RFC](../10-architecture/0002-2026-09-07-w2-bridge-scalar-ascend-contract.zh-CN.md)。
