# PyPTO-X W2 完成交接快照

归档序号：`0003`

归档日期：2026-09-07（Asia/Shanghai）

状态：`W2_COMPLETE_W2B_READY`

## 冻结点

```text
base        = 2ab2f2ca59bd86dee654b55a0e97b7d25af966e9
integration = b017b4bb3e189ba3f50ae777461f0574decf26ba
branch      = port/pypto-x-integration
```

完整 task→commit 映射见 [开发集成锁](../../configs/development_lock.yaml)。

## 已完成能力

- Tensor/PIL duck-typed bridge，保留稳定 SSA、重复 operand 和结构化 region。
- 已审计 Tensor callable 名称到 portable op 的白名单映射；unknown op/effect 显式保留和诊断。
- PIL scope load/store 的 portable SSA 消解，TensorAnnotation dtype/shape 与常见 `DT_*` 规范化。
- CPU scalar compiler/runtime：constant、identity、add/sub/mul/float-div/neg、reduce sum/max、二维 matmul。
- BF16/FP16 RNE host value、artifact digest/target/version 校验、公共 LaunchRequest 输出写回。
- Ascend dependency-injected adapter seam；公共导入不主动加载 CANN，真实 hook 缺失时结构化 unavailable。

## 验证证据

- W2 三个 task 的唯一一次 host smoke：全部 `PASS`。
- integration 同一进程全量测试：`66 passed`。
- 隔离链 `PIL → CoreProgram → Artifact → LaunchRequest → CPU scalar → differential`：`PASS`，dtype 检查保持开启。
- Python 3.7 AST：42 个新增 Python 文件 `PASS`。
- setuptools package discovery：`PASS`。
- integration smoke：`PASS`，同时 compileall `pypto` 与 `pypto_pro`。

## 诚实边界

- 普通源码环境直接导入 `pypto.frontend.pil_core_bridge` 仍会先执行 native-oriented package initializer；已复现失败，不能用隔离测试掩盖。
- W2 的 PIL bridge 用结构等价 fake 验证；真实 native C++ IR bridge 尚未连通。
- Ascend 仅 seam/fake hook 单测通过；CANN toolkit、Bisheng 和 NPU 回归仍阻塞。
- CPU scalar 是小规模正确性 golden，不是性能 backend，也没有模型覆盖声明。

## 下一步

先执行 [portable bootstrap](../10-architecture/0003-2026-09-07-portable-bootstrap-contract.zh-CN.md)，用显式 `PYPTO_X_PORTABLE_ONLY=1` 完成普通导入与 scalar add 子进程测试；通过后再开始 vector-common/AVX2。
