# PyPTO-X W4 SVE256 完成交接快照

归档序号：`0008`

归档日期：2026-09-08（Asia/Shanghai）

状态：`W4_SVE256_COMPLETE_GPU_COMMON_READY`

## 冻结点

```text
base        = 79a7daa1052775651b22e797c346651c70b60070
integration = 5e42eed7c432034db02c2d339e67789afd419ae9
branch      = port/pypto-x-integration
```

完整 commit 与验证字段见 [开发集成锁](../../configs/development_lock.yaml)。

## 已完成能力

- Linux AArch64 `getauxval(HWCAP/HWCAP2)` + `prctl(PR_SVE_GET_VL)` probe。
- x86 开发机显式 QEMU execution mode；默认 runtime 只尝试 native，不静默模拟。
- scalable SVE codegen，SVE256 target 在 probe 与 runner 两层验证实际 VL=32 bytes。
- 复用 vector-common 的 8-lane logical plan，热点使用 `whilelo`、predicate load/store 与 z/p 寄存器。
- FP32/BF16 constant、identity、elementwise、reduce sum/max 与二维矩形 matmul。
- BF16 输入转 FP32、FP32 accumulation、最终 BF16 RNE；SVE2 不进入 emitting required features。
- ELF runner wire protocol、checked arithmetic、精确 payload、零维和 malformed request 门禁。
- ELF64/little-endian/AArch64 header、内嵌 source/variant/protocol/VL marker 和 content-addressed 发布。
- HostTensor、FlatBuffer、raw address、ctypes、NumPy FP32/BF16 bits buffer ABI。
- guarded unaligned-buffer/canary self-test；初始化与检查不使用未对齐标量 C load/store。

## 验证证据

- task 唯一一次 QEMU smoke：`PASS`。
- SVE 专属测试：`59 passed`；x86 + SVE：`168 passed`。
- integration 全量测试：`264 passed in 54.77s`，进程退出码 0。
- Python 3.7 AST：累计 73 个新增 Python 文件 `PASS`。
- compileall、setuptools package discovery（35 packages）、`git diff --check` 与行宽：`PASS`。
- probe：`emulated=true`、SVE=1、SVE2=1、VL=32 bytes。
- VL128/VL512 直接执行 artifact 均返回 exit 16。
- 热函数：`whilelo=7`、`ld1w=14`、`st1w=7`、z=42、p=17、NEON v/q=0。
- guarded canary、全负 reduce-max 与 19 元素 scalar differential：`PASS`。

主要证据：

- `../worktrees/_meta/pypto-x/integration-w4-sve256-final/logs/20260908T055108Z-integration-validation.json`
- `../worktrees/_meta/pypto-x/integration-w4-sve256-final/smoke/20260908T055308Z/smoke.log`

## 限制与下一步

- 当前只有 QEMU 功能、ABI 与汇编证据，不能代表鲲鹏性能、缓存、NUMA 或功耗。
- 鲲鹏 SVE256 真机能力与性能验证仍待机器到位。
- SVE2 仅探测到 capability，尚无 SVE2 专属 emitting lowering。
- 下一步冻结 GPU common IR/runtime ABI，再分别实现 CUDA/NVVM 与 HIP/ROCDL。
