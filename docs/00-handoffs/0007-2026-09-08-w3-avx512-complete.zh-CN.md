# PyPTO-X W3 AVX-512 完成交接快照

归档序号：`0007`

归档日期：2026-09-08（Asia/Shanghai）

状态：`W3_COMPLETE_W4_SVE256_READY`

## 冻结点

```text
base        = 68e400b3fc3ff75fa004dd15457997b6a7709a70
integration = 79a7daa1052775651b22e797c346651c70b60070
branch      = port/pypto-x-integration
```

完整 commit 与验证字段见 [开发集成锁](../../configs/development_lock.yaml)。

## 已完成能力

- 真实 CPUID + OSXSAVE/XGETBV 探测 AVX-512F/DQ/BW/VL/VNNI/BF16 与 OS ZMM/opmask state。
- 复用 vector-common plan 的 16-lane ZMM lowering，elementwise/matmul 使用 opmask masked tail。
- Clang base、FMA、BF16、VNNI native artifact；高级指令只在对应 required-feature 变体出现。
- FP32/BF16 elementwise、reduce sum/max、二维矩形 matmul，以及 BF16 FP32 accumulation + 最终 RNE。
- BF16 `vdpbf16ps` 与 target-specific VNNI `vpdpbusd` helper 的真实运行 golden。
- advanced AVX-512 → base AVX-512 → AVX2 的语义绑定 fallback，选择发生在不支持 artifact 的 `dlopen` 前。
- 延续 AVX2 的 content-addressed 只读 artifact、marker/digest/ABI、symlink/hardlink 与 buffer ABI 门禁。
- DQ/BW/VL capability 可探测，但在没有 emitting lowering 前 codegen 显式 unsupported，不伪装支持。

## 验证证据

- task 唯一一次 host smoke：`PASS`。
- AVX-512 专属测试：`68 passed`；AVX2 + AVX-512：`109 passed`。
- integration 全量测试：`205 passed in 41.62s`，进程退出码 0。
- Python 3.7 AST：累计 63 个新增 Python 文件 `PASS`。
- compileall、setuptools package discovery（32 packages）、`git diff --check` 与行宽：`PASS`。
- 全负数 35 元素 reduce-max：`[-1.0, -36.0]`。
- FMA 3×5×17 matmul scalar differential：最大绝对误差约 `3.67e-8`。
- BF16 `n=17, k=3` full/tail golden：`20.25/34.0`。
- VNNI u8×s8 golden：`22880`。

integration 反汇编摘要：

```text
base:          zmm=864, opmask=79, vfmadd=0,  vpdp=0, vdpbf16=0
FMA:           zmm=833, opmask=78, vfmadd=19
BF16:          zmm=845, opmask=75, vdpbf16=1
VNNI:          zmm=979, opmask=79, vpdp=1
AVX2 fallback: ymm=396, zmm=0, evex=0
```

主要证据：

- `../worktrees/_meta/pypto-x/integration-w3-avx512-final2/logs/20260908T040842Z-integration-validation.json`
- `../worktrees/_meta/pypto-x/integration-w3-avx512-final2/smoke/20260908T041123Z/smoke.log`

## 审查中修复的问题

- 修复 all-negative `reduce_max` 尾块被 mask-zero lane 抬高为 0。
- fallback 增加 canonical CoreProgram digest、entrypoint、ABI、triple 与安全 feature 绑定。
- DQ/BW/VL 从“只增加编译 flag”改为没有 emitting lowering 时显式拒绝。
- 修复 AVX2 objdump 测试误把含 `avx512` 的目录路径当作 AVX-512 指令证据。
- 所有实际高级指令与 fallback 测试先经过真实硬件 capability 门禁，fake capability 不执行未知 ISA。

## 限制与下一步

- 当前数据来自 KVM，只证明正确性、分层派发和指令生成，不作为生产性能结论。
- DQ/BW/VL 尚无 emitting lowering；VNNI helper 尚未接入公共 Core op。
- 下一步从该冻结点创建 `cpu-sve256`，先在 QEMU 验证 SVE/SVE2 与 predicate tail；鲲鹏真机到位后再补原生性能与 PMU。
